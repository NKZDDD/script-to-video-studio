# 环节映射表｜V3.4 十七章 → 本分支的可执行环节

这份表是 `v34-cinematic` 分支的施工图。左边是 skill 的章，右边是程序里真实存在的
一个环节（有 id、有模板、有必需输出字段、有依赖）。

看这份表要抓三件事：**哪些章合并成一个 LLM 调用**、**每集/每段各要调几次**、
**哪几条明确不做**。

---

## 一、为什么不是一章一个环节

17 章里有三类东西，混在一起会算错成本：

| 类型 | 章 | 在程序里是什么 |
|---|---|---|
| 配置冻结 | 0 | **不调模型**。项目创建时的参数，落在 `meta.json` |
| 分析与设计 | 1–13、16 | 调 LLM，产出 JSON |
| 解析与装配 | 14 | **不调模型**。就是 `build_tasks` + Manifest 核对 |
| 提示词编译 | 15、17 | 调 LLM，产出可直接投喂的提示词正文 |
| 生产执行 | — | 调出图/出片服务商（skill 不算章，但程序必须有） |

所以「17 章」落到程序里是 **13 次 LLM + 2 处程序装配 + 4 处生产执行 + 交付**。

## 二、合并判定

一个 LLM 调用应该产出**一件完整、能独立校验的产物**。按这个标准合并了四处：

| 合并 | 章 | 理由 |
|---|---|---|
| `n1` 源解析 + 故事真相 | 1 + 2 | 都是全剧一次、都在回答「发生了什么」。分两次要把实体表再发一遍 |
| `n7` CVS + VT | 9 + 10 | VT 的定义就是「连接两个 CVS」，分开必然要互相引用 |
| `n8` 摄影 + 剪辑时间 | 11 + 12 | 镜头和它的时长/转场是同一件事，分开会算出容纳不下的 SEG |
| `n12` 执行计划 + 视频提示词 | 16 + 17 | 16 是 17 的骨架，分两次等于把时间轴写两遍 |

**刻意不合并**的两处：

- **3 叙事结构** 和 **4 人物世界规则**：前者决定切集，后者是全剧长期规则。
  合并会让「改人物设定」触发重切集。
- **5 资产** 和 **6 空间**：资产按对象、空间按 Location，两套编号体系。
  V3.4 第 6 章明写「不用独立生图决定几何/人物位置」，混在一起容易越界。

## 三、环节表

范围列：`全剧` = 只跑一次；`逐集` = 每集一次；`逐段` = 每段一次。

| id | 环节 | 章 | kind | 范围 | 依赖 | 必需输出字段 |
|---|---|---|---|---|---|---|
| `n0` | 项目初始化与配置冻结 | 0 | program | 全剧 | — | 写 `meta.json`：`project_id` / `seg_duration` / `target_video_model` / `transition_execution_mode` / `native_multishot_support` / 能力探测结果 |
| `n1` | 源解析与故事真相 | 1+2 | llm | 全剧 | — | `entities[]` `events[]` `story_truth` `reality_threads[]` `episode_ranges[]` |
| `n2` | 叙事结构 | 3 | llm | 全剧 | n1 | `scenes[]`（含 `scene_id`/`objective`/`turn`/`outcome`/`entry_state`/`exit_state`）`beats[]`（含 `meaningful_change`/`state_delta`） |
| `n3` | 人物与世界规则 | 4 | llm | 全剧 | n1 | `characters[]` `world_rules[]` `performance_constraints[]` |
| `n4` | 资产系统 | 5 | llm | 逐集 | n1,n2,n3 | `assets[]`（`asset_id`/`family`/`parent_asset_id`/`reference_assets[]`/`dependency_order`/`decision`）`costume_contracts[]` `prop_specs[]` `prop_instances[]` |
| `n5` | 空间主表 | 6 | llm | 逐集 | n2,n4 | `spatial_masters[]`（`world_origin`/`axis`/`unit`/`zones[]`/`anchors[]`（含 XYZ）/`routes[]`/`landmarks[]`/`fixed_structures[]`）`loc_views[]` |
| `n6` | 连续性总账 | 7 | llm | 逐集 | n2,n4,n5 | `ledger[]`（`event_id`/`affected_entity`/`state_dimension`/`previous_value`/`delta`/`result_value`/`activation_event`/`persistence_class`/`deactivation`） |
| `n7` | 导演设计 | 8 | llm | 逐集 | n2,n3,n5,n6 | `scene_directing[]` `beat_directing[]` `blocking[]`（Zone/Anchor/XYZ/朝向/Route）`performance_intent[]` |
| `n8` | CVS 与视觉过渡 | 9+10 | llm | 逐集 | n4,n5,n6,n7 | `cvs[]`（**不含 camera 任何字段**）`vt[]`（`source_cvs`/`trigger_event`/`target_cvs`/`irreversible_result`） |
| `n9` | 摄影镜头与剪辑时间 | 11+12 | llm | 逐集 | n7,n8 | `shots[]`（`shot_size`/`camera_position_xyz`/`screen_direction`/`estimated_duration`）`transitions[]`（`mechanism`/`cinematic_grammar`/`execution_mode`/`cut_at_or_switch_point`/`shield_type`）`timing_plan[]` |
| `n10` | SEG 包装 | 13 | llm | 逐集 | n9 | `segs[]`（`seg_id`/`duration`/`included_shots[]`/`entry_cvs`/`exit_cvs`/`model_native_transition_ids[]`/`transition_ownership`/`boundary_rationale`） |
| `n11` | SCSTATE 编译 | 9视觉物化 | llm | **逐段** | n4,n5,n8,n10 | `scstates[]`（`scstate_id`/`source_cvs`/`reference_assets[]`/`prompt`） |
| `n12` | 故事板包编译 | 14+15 | llm | **逐段** | n9,n10,n11 | `sbpkg[]`（`kf[]` 含 KF 合同、`reference_order[]`、`storyboard_prompt`） |
| `n13` | 视频执行与提示词 | 16+17 | llm | **逐段** | n9,n10,n12 | `video_plan[]`（`windows[]` 含绝对秒数/Transition Window/Switch Point/Reveal Envelope）`video_prompt` |
| `n14` | 漏洞审计 | 07-ref | llm | 逐集 | 全部 | `findings[]`（对着 07 的自检清单逐条判） |

### 生产执行（skill 不算章，程序必须有）

| id | 干什么 | kind | 输入 | 排在哪 |
|---|---|---|---|---|
| `p1` | 资产图生产 | image | n4 的资产 + 资产提示词 | 所有集 n4 之后（资产全剧共享，按依赖分层） |
| `p2` | SCSTATE 图生产 | image | n11 | p1 之后（SCSTATE 要拿资产图当参考） |
| `p3` | 故事板生产 | image | n12 | p2 之后 |
| `p4` | 视频生产 | video | n13 | p3 之后 |
| `d1` | 人工复核清单 | local | n14 + 全部产物 | p4 之后 |
| `d2` | 拼接交付 | local | p4 | 最后 |

**注意 `p2` 是 V6.1 完全没有的一层。** 现在故事板直接拿一堆原子资产当参考图
（SEG01 传了 5 张），V3.4 要求先把「这一刻整个世界」合成一张 SCSTATE，
故事板主要参考它。这是减少参考图打架的关键一步。

## 四、成本对照

按一集 6 段算：

| | V6.1（main） | V3.4（本分支） |
|---|---|---|
| 全剧一次 | 1 次（环节1） | 3 次（n1/n2/n3） |
| 逐集 | 6 次（s2–s6） | 8 次（n4–n10、n14） |
| 逐段 | 2 次 × 6 = 12 | 3 次 × 6 = 18 |
| **一集合计** | **18 次** | **26 次** |
| 出图 | 资产 + 故事板 | 资产 + **SCSTATE** + 故事板 |

**贵约 45%，多一层 SCSTATE 出图。** 换来的是参考图打架、跨段状态丢失、
人物位置漂移这三类问题从「靠肉眼发现」变成「有结构可校验」。

值不值得，跑完一集对比再定 —— 这也是为什么不合回 main。

## 五、明确不做的（写下来免得反复讨论）

| 不做 | V3.4 出处 | 为什么 |
|---|---|---|
| **Geometry Proxy 出图** | 第 6 章 | 要求先建 3D/2.5D 空间代理再逐机位投影。出图模型做不出可靠几何代理，不是提示词能解决的。**但坐标系/Zone/Anchor XYZ/Route 全部保留**（`n5` 就是它，而且 main 上已经有 `space_masters` 了） |
| **Canonical Revision 不可变** | 第 10 章 | 「改内容必须建 R02 不能覆盖」和「磁盘是唯一真相、重跑覆盖、做过就跳过」是冲突的模型。要上得连带改产物布局和续跑语义 |
| **项目命名空间长 ID** | 第 10 章 | `PRJ_NOVA__CHAR_001_PH01_LK01_CT01_R01`。先沿用 `C001`/`ST007` 短 ID —— 长 ID 的收益是多项目共库，你现在一个项目一个目录，收益为零，成本是所有产物和文件名重构 |
| **SHA-256 Fingerprint 校验** | 第 14 章 | 参考图解析已经有「文件不在就硬停」+「Image 编号必须对上」两道。指纹解决的是「文件被换过」，等真遇到再加 |

**这四条都是可以后补的加法，不是必须现在决定的地基。** 真遇到对应的实际问题
（多视角拼不起来、旧图污染下游、多项目共库、文件被换）再回来评估。

## 六、施工顺序

每一步做完系统都还能跑，不留半截：

1. **环节表 + 依赖图**（`STAGES` / `_LLM_SPEC`）—— 先让流程页能列出 17 个环节
2. **n1–n3 全剧三环节**（模板 + 校验）—— 能切集就能验证方向
3. **n4–n6 资产/空间/连续性** —— 这三个产物是后面所有环节的输入
4. **n7–n10 导演/CVS/镜头/SEG** —— 到这里文字部分完整
5. **n11 SCSTATE + p2 出图** —— 新增的那一层，单独验
6. **n12–n13 故事板 + 视频**（含 Model-Native 多镜头）—— 招牌功能
7. **n14 审计 + d1/d2 交付**

第 6 步是整件事的价值所在。前 5 步都是为它铺路 —— 如果中途要停，
停在第 5 步之后仍然有意义（SCSTATE 那一层单独就能改善参考图打架）。
