# 小裴 API 视频插件接口文档

**插件版本：3.3.53**  
**适用目录：** `07.13更新小裴视频插件3.1.0（自动更新功能）`  
**更新时间：2026-08-15**

> 本文档是按当前插件 `main.py` 的实际请求构造逻辑整理的开发联调文档。它描述的是插件发给“Base URL（中转）”的协议，而不是任一上游厂商的原生 REST 文档。不同分支的字段不可互换。

## 目录

1. [通用约定](#通用约定)
2. [创建、轮询、下载与响应兼容](#创建轮询下载与响应兼容)
3. [模型总表](#模型总表)
4. [GROK 系列](#grok-系列)
5. [稳定渠道](#稳定渠道)
6. [低价与轮换渠道](#低价与轮换渠道)
7. [惊喜渠道](#惊喜渠道)
8. [扩展渠道](#扩展渠道)
9. [字段速查和错误排查](#字段速查和错误排查)

---

## 通用约定

### 基础地址与请求头

插件配置中的 `base_url` 为中转根地址，末尾 `/` 会自动移除。以下示例的 `BASE` 均指该地址。

```http
Authorization: Bearer <用户配置的 API Key>
Content-Type: application/json
```

除非某个分支单独说明，创建接口为 `POST`，查询接口为 `GET`。插件通过电脑本机代理设置请求；`proxy_url` 留空时不强行指定代理。

所有后续 JSON 请求体均可按下面的 cURL 外壳调用，只需把 `<PATH>` 换为该章节的创建路径，把 `payload.json` 换成对应示例内容：

```bash
curl -X POST "https://<BASE><PATH>" \
  -H "Authorization: Bearer <API_KEY>" \
  -H "Content-Type: application/json" \
  --data @payload.json
```

查询统一使用：

```bash
curl "https://<BASE><QUERY_PATH>/<TASK_ID>" \
  -H "Authorization: Bearer <API_KEY>"
```

### 素材来源与上传结果

插件从字字动画项目中读取原始素材，不使用画布预览缩略图。图片进入视频插件前会等比例压缩，**最大边长 1024px**，然后按渠道要求转换成以下一种形式：

| 形式 | 格式 | 使用的分支 |
|---|---|---|
| 公网 URL | `https://...`，可被中转/上游直接下载 | SD 多参考、火山、H3、Omni、Veo 等 |
| Data URL | `data:image/jpeg;base64,...` | GROK、Horse 官方等 |
| 中转上传 URL | 插件先上传素材，再把返回 URL 写入请求 | 需要公网媒体的 SD/H3/火山分支 |

视频和音频也必须是中转可访问的 URL。不要把电脑绝对路径、临时预览图路径或裸 Base64 字符串放进 URL 字段。

### 模式中的素材位置

插件界面按顺序取图：

| 模式 | 图片顺序 |
|---|---|
| 首帧生成视频 | 第 1 张为首帧 |
| 首尾帧生成视频 | 第 1 张为首帧，第 2 张为尾帧 |
| 多参考图生成视频 | 所有图片均为参考图，顺序保留 |
| 首帧+参考图生成视频 | 第 1 张首帧，后续图片参考图 |

每个分支只会把自身支持的素材类型写入请求。比如只支持单图的快乐马惊喜分支不会把第二张图伪装成“尾帧”。

---

## 创建、轮询、下载与响应兼容

### 创建成功响应

中转可立即给出成片 URL，也可异步返回任务 ID。插件识别以下常见任务 ID（也会递归检查 `data`、`result`、`output` 等对象和数组）：

```json
{"id":"task_123","status":"pending"}
```

```json
{"task_id":"task_123","status":"queued"}
```

```json
{"data":{"task":{"id":"task_123"}}}
```

可识别键：`id`、`task_id`、`taskId`、`request_id`、`requestId`、`video_id`、`videoId`、`job_id`、`jobId`。

### 状态与成片 URL

成功状态兼容：`completed`、`succeeded`、`success`、`done`、`finished`、`completed_success`。失败状态兼容：`failed`、`failure`、`error`、`cancelled`、`canceled`、`rejected`、`expired`。

视频链接会从根对象及嵌套对象中查找：`video_url`、`url`、`download_url`、`result_url`、`final_url`、`content_url`、`output_url`、`file_url`、`video_uri`、`content`、`file`。示例：

```json
{
  "status": "completed",
  "data": {"content": {"video_url": "https://cdn.example.com/result.mp4"}}
}
```

### 轮询与下载

| 协议类别 | 查询 | 没有 URL 时的下载回退 |
|---|---|---|
| 标准视频 | `GET /v1/videos/{task_id}` | `GET /v1/videos/{task_id}/content` |
| 多参考 | `GET /v1/video/generations/{task_id}` | 通常返回 URL；H3 回退 `/v1/videos/{task_id}/content` |
| Horse 官方 | `GET /v1/videos/{task_id}`（可含厂商路径前缀） | `/v1/videos/{task_id}/content` |

轮询间隔通常 5 秒（Omni/Veo 为 6 秒，少数历史兼容路径为 3 秒），默认总超时由插件 `timeout` 控制，未填写为 3600 秒，最低有效等待窗口 120 秒。成功后插件下载完整 MP4 到任务输出目录；不会把轮询中的局部文件当作成片。

---

## 模型总表

| 分组 | 分支 | 变体/实际 `model` | 时长 | 分辨率 | 模式 |
|---|---|---|---|---|---|
| GROK | GROK 1.0-video（可多参） | `grok-imagine-1.0-video`、`grok-1.0-官转接口`、`grok-1.0-备用接口` | 6、10 | 720p | 文生、首帧、多参考图 |
| GROK | GROK1.5-Preview（可多参） | `grok-imagine-video-1.5-preview`、`grok-1.5-官转接口`、`grok-1.5-备用接口`、`grok-1.5-多参接口` | 6、10、15 | 720p | 文生、首帧、多参考图 |
| 稳定 | Horse官方快乐马不卡脸（按秒） | `happyhorse-1.1-{t2v|i2v|r2v}-{720p|1080p}` | 4-15 | 720p/1080p | 由变体锁定 |
| 稳定 | Minimax-h3 | `开源h3-480p`、`开源h3-720p`、`开源h3-1080p`、`开源h3-2k` | 5-15 | 变体锁定 | 文生、首帧、首尾帧、多参考 |
| 稳定 | 【火山官方】sd稳定版 | 火山官方 2.5 480/720、2.0 mini 480/720 | 2.5: 4-30；mini: 4-15 | 变体锁定 | 首帧、首尾帧、多参考 |
| 稳定 | sd-2.5-不卡脸（按秒） | 480/720，含备用各一 | 4-29 | 变体锁定 | 文生、首帧、首尾帧、多参考、首帧+参考 |
| 稳定 | sd2.0全系列不卡脸（按秒+按次） | 720/1080 mini、fast、满血 | 4-15 | 变体锁定 | 文生、首帧、首尾帧、多参考、首帧+参考 |
| 低价 | sd2.0-全系ad渠道卡脸（按次） | 480/720/1080 fast/满血，16x9/9x16 | 固定 15 | 变体锁定 | 文生、首帧、首尾帧、多参考 |
| 低价 | sd-满血不卡脸-933（按秒+按次） | 480/720，按秒/按次 | 4-15 | 变体锁定 | 文生、首帧、首尾帧、多参考、首帧+参考 |
| 低价 | sd轮换渠道不卡脸（动态调价） | 5 个按次/按秒/2.5 变体 | 见后文 | 固定 720p | 文生、首帧、首尾帧、多参考 |
| 扩展 | sd-720满血-900（不售后） | 同名 | 固定 15 | 固定 720p | 多参考图 |
| 惊喜 | sd-2.0惊喜渠道（卡脸） | 480/720/1080 full/fast | 4-15 | 变体锁定 | 文生、首帧、首尾帧、多参考、首帧+参考 |
| 惊喜 | sd-2.0惊喜渠道（不卡脸） | 480/720 满血 | 4-15 | 变体锁定 | 文生、首帧、首尾帧、多参考、首帧+参考 |
| 惊喜 | 快乐马1.1惊喜渠道（不卡脸） | `快乐马1.1（不卡脸）惊喜渠道` | 4-15 | 720/1080 | 文生、首帧 |
| 惊喜 | 可灵-3.0-omni惊喜渠道（不卡脸） | `可灵-3.0-omni（不卡脸）惊喜渠道` | 4-15 | 720/1080 | 文生、首帧、多参考 |
| 扩展 | omni | 生成/编辑四个 `omni-fast-*` 变体 | 固定 10 | 固定 720p | 文生、首帧、首尾、多图、视频编辑 |
| 扩展 | veo视频生成 | `veo视频生成` | 4、6、8 | 固定 720p | 文生、首帧、首尾 |

---

## GROK 系列

### 1. GROK 1.0-video（可多参）

**接口：** `POST /v1/videos`，查询 `GET /v1/videos/{task_id}`。支持比例 `16:9`、`9:16`、`3:2`，固定 720p，时长仅 `6` 或 `10`。

| 字段 | 类型 | 插件写入 | 说明 |
|---|---|---|---|
| `model` | string | 是 | 见下方映射 |
| `prompt` | string | 是 | 视频提示词 |
| `duration`、`video_length` | integer | 是，值相同 | 兼容中转的时长字段 |
| `aspect_ratio` | string | 是 | 三种比例之一 |
| `resolution` | string | 是 | `720p` 的中转兼容值 |
| `video_config` | object | 是 | 内含相同的 `video_length`、`aspect_ratio`、`resolution`、`preset:"normal"` |
| `image` | string | 首帧模式 | 一张 `data:image/...` 图片 |
| `reference_images` | string[] | 多参考模式 | 最多 7 张 `data:` 图片 |

| UI 变体 | 发给中转的 `model` |
|---|---|
| `grok-1.0-video（按次）` | `grok-imagine-1.0-video` |
| `grok-1.0-官转接口（按秒）` | `grok-1.0-官转接口` |
| `grok-1.0-备用接口（按秒）` | `grok-1.0-备用接口` |

**文生示例：**

```json
{
  "model": "grok-imagine-1.0-video",
  "prompt": "雨夜城市，镜头缓慢推进",
  "duration": 6,
  "video_length": 6,
  "aspect_ratio": "16:9",
  "resolution": "720p",
  "video_config": {"video_length": 6, "aspect_ratio": "16:9", "resolution": "720p", "preset": "normal"}
}
```

首帧时追加 `"image":"data:image/jpeg;base64,..."`；多参考时改为 `"reference_images":["data:image/jpeg;base64,...", "data:image/png;base64,..."]`。两者不能由插件同时写入。

### 2. GROK1.5-Preview（可多参）

**接口：** `POST /v1/videos`，查询 `GET /v1/videos/{task_id}`。比例 `16:9`、`9:16`、`3:2`，固定 720p；时长仅 `6`、`10`、`15`。图片最多 7 张，均为 Data URL 对象。

| 字段 | 类型 | 说明 |
|---|---|---|
| `model`、`prompt` | string | 必填 |
| `seconds` | string | `"6"`、`"10"` 或 `"15"` |
| `size` | string | 由比例生成，例如 `1280x720` 或 `720x1280` |
| `reference_images` | object[] | 有图时写入，每项必须是 `{"url":"data:image/...;base64,..."}` |

```json
{
  "model": "grok-1.5-多参接口",
  "prompt": "参考两张人物图，保持角色一致",
  "seconds": "10",
  "size": "1280x720",
  "reference_images": [
    {"url": "data:image/jpeg;base64,..."},
    {"url": "data:image/png;base64,..."}
  ]
}
```

文生时省略 `reference_images`；首帧模式同样使用仅含 1 项的 `reference_images`，不是 `image_url`。可用实际模型：`grok-imagine-video-1.5-preview`、`grok-1.5-官转接口`、`grok-1.5-备用接口`、`grok-1.5-多参接口`。

---

## 稳定渠道

### 3. Horse官方快乐马不卡脸（按秒）

**接口：** `POST /v1/videos`，查询 `GET /v1/videos/{task_id}`。时长 4-15 秒，比例为 `16:9`、`9:16`、`1:1`、`4:3`、`3:4`、`4:5`、`5:4`、`9:21`、`21:9`。

| 变体 | 固定模式 | 输出分辨率 |
|---|---|---|
| `happyhorse-1.1-t2v-720p` / `-1080p` | 文生 | 720P / 1080P |
| `happyhorse-1.1-i2v-720p` / `-1080p` | 首帧 | 720P / 1080P |
| `happyhorse-1.1-r2v-720p` / `-1080p` | 多参考图 | 720P / 1080P |

```json
{
  "model": "happyhorse-1.1-r2v-720p",
  "prompt": "参考图片中人物，电影感运动镜头",
  "parameters": {"duration": 8, "resolution": "720P", "watermark": false, "ratio": "16:9"},
  "reference_images": ["data:image/jpeg;base64,...", "data:image/jpeg;base64,..."]
}
```

文生不带图片字段。首帧使用 `"image_url":"data:image/jpeg;base64,..."`，且插件不传 `parameters.ratio`，由首帧决定画幅。多参考最多 9 图。

### 4. Minimax-h3

**接口：** `POST /v1/video/generations`，查询 `GET /v1/video/generations/{task_id}`，成片 URL 缺失时下载 `GET /v1/videos/{task_id}/content`。时长 5-15 秒，固定 `fps:24`；比例支持 `16:9`、`9:16`、`1:1`、`4:3`、`3:4`、`21:9`。

| 变体 `model` | 固定交付分辨率 |
|---|---|
| `开源h3-480p` | 480p |
| `开源h3-720p` | 720p |
| `开源h3-1080p` | 1080p |
| `开源h3-2k` | 2K |

| 字段 | 类型/限制 | 说明 |
|---|---|---|
| `model`,`prompt`,`aspect_ratio`,`duration`,`fps` | 必填 | `fps` 固定为 24 |
| `reference_images` | 0-9 | `[{"url":"...","role":"..."}]` |
| `reference_videos` | 0-3 | `[{"url":"..."}]`，仅多参考 |
| `reference_audios` | 0-3 | `[{"url":"..."}]`，仅多参考 |

```json
{
  "model": "开源h3-720p",
  "prompt": "首尾姿态连续，镜头稳定推进",
  "aspect_ratio": "9:16",
  "duration": 8,
  "fps": 24,
  "reference_images": [
    {"url": "https://cdn.example.com/first.jpg", "role": "first_frame"},
    {"url": "https://cdn.example.com/last.jpg", "role": "last_frame"}
  ]
}
```

首帧仅传一项 `role:"first_frame"`。多参考图的所有图片都传 `role:"reference_image"`，可同时附带 `reference_videos` 与 `reference_audios`。

### 5. 【火山官方】sd稳定版

**接口：** `POST /v1/videos`，查询 `GET /v1/videos/{task_id}`，完成但无链接时 `GET /v1/videos/{task_id}/content`。本分支**没有文生视频模式**，仅首帧、首尾帧、多参考。素材通过标准 `content` 数组传递。

| UI/实际 `model` | 固定 `resolution` | 时长 | 素材限制 |
|---|---:|---:|---|
| `火山官方2.5-480p` | 480p | 4-30 | 图30、视频10、音频10 |
| `火山官方2.5-720p` | 720p | 4-30 | 图30、视频10、音频10 |
| `火山官方2.0-480p-mini` | 480p | 4-15 | 图9、视频3、音频3 |
| `火山官方2.0-720p-mini` | 720p | 4-15 | 图9、视频3、音频3 |

公共字段：`model`、`content`、`generate_audio:true`、`duration`、`watermark:false`、`resolution`。多参考额外传 `ratio`；首帧和首尾帧**禁止传 `ratio`**，输出比例跟随首帧，否则火山会返回 `InvalidParameter.TaskTypeConstraint`。

**首尾帧完整示例：**

```json
{
  "model": "火山官方2.5-480p",
  "content": [
    {"type": "text", "text": "保持人物身份一致，从首帧自然过渡到尾帧"},
    {"type": "image_url", "image_url": {"url": "https://cdn.example.com/first.jpg"}, "role": "first_frame"},
    {"type": "image_url", "image_url": {"url": "https://cdn.example.com/last.jpg"}, "role": "last_frame"}
  ],
  "generate_audio": true,
  "duration": 8,
  "watermark": false,
  "resolution": "480p"
}
```

**多参考完整示例：**

```json
{
  "model": "火山官方2.5-720p",
  "content": [
    {"type": "text", "text": "使用图片身份、视频运镜和音频节奏生成广告短片"},
    {"type": "image_url", "image_url": {"url": "https://cdn.example.com/ref-1.jpg"}, "role": "reference_image"},
    {"type": "image_url", "image_url": {"url": "https://cdn.example.com/ref-2.jpg"}, "role": "reference_image"},
    {"type": "video_url", "video_url": {"url": "https://cdn.example.com/motion.mp4"}, "role": "reference_video"},
    {"type": "audio_url", "audio_url": {"url": "https://cdn.example.com/music.mp3"}, "role": "reference_audio"}
  ],
  "generate_audio": true,
  "duration": 12,
  "watermark": false,
  "resolution": "720p",
  "ratio": "16:9"
}
```

首帧示例与首尾帧相同，只保留一项 `role:"first_frame"`。多参考中所有图片均为 `reference_image`，不要混入 `first_frame`/`last_frame`。

### 6. sd-2.5-不卡脸（按秒）

**接口：** `POST /v1/videos`，查询 `GET /v1/videos/{task_id}`。时长 4-29，比例 `16:9`、`9:16`、`1:1`；图最多 30，视频和音频各最多 10。

实际 `model` 是所选变体：`sd-2.5-480p不卡脸(按秒)`、`sd-2.5-720p不卡脸(按秒)`、`sd-2.5-480p不卡脸(按秒)-备用`、`sd-2.5-720p不卡脸(按秒)-备用`。分辨率由模型锁定，插件不发送 `resolution`。

```json
{
  "model": "sd-2.5-720p不卡脸(按秒)",
  "prompt": "综合所有参考素材生成连续镜头",
  "duration": 20,
  "aspect_ratio": "16:9",
  "images": ["https://cdn.example.com/1.jpg", "https://cdn.example.com/2.jpg"],
  "videos": ["https://cdn.example.com/ref.mp4"],
  "audios": ["https://cdn.example.com/ref.mp3"]
}
```

文生只保留前四字段。首帧使用 `images` 一项；首尾两项；多参考为任意多项；首帧+参考同样由图片顺序表达，不另传 `first_frame_url`。

### 7. sd2.0全系列不卡脸（按秒+按次）

**接口：** `POST /v1/videos`，查询 `GET /v1/videos/{task_id}`。时长 4-15，比例 `16:9`、`9:16`、`1:1`；图9、视频3、音频3。

模型为所选变体：`sd2.0-720mini-不卡脸（按秒）`、`sd2.0-720fast-不卡脸（按秒）`、`sd2.0-720满血-不卡脸（按秒）`、`sd2.0-720满血（按次）不卡脸`、`sd2.0-720fast（按次）不卡脸`、`sd2.0-1080mini-不卡脸（按秒）`、`sd2.0-1080fast-不卡脸（按秒）`、`sd2.0-1080满血-不卡脸（按秒）`。

```json
{
  "model": "sd2.0-720满血-不卡脸（按秒）",
  "prompt": "角色向镜头走来，保留参考图身份",
  "duration": 8,
  "metadata": {"ratio": "9:16", "enableSound": "on", "modeType": "frames2video"},
  "images": ["https://cdn.example.com/first.jpg", "https://cdn.example.com/last.jpg"],
  "videos": ["https://cdn.example.com/camera.mp4"],
  "audios": ["https://cdn.example.com/music.mp3"]
}
```

`metadata.modeType` 规则：文生 `text2video`；首帧/首帧+参考 `image2video`；首尾帧 `frames2video`；纯视频参考可省略。`enableSound` 固定为字符串 `"on"`。

---

## 低价与轮换渠道

### 8. sd2.0-全系ad渠道卡脸（按次）

**接口：** `POST /v1/videos`，查询 `GET /v1/videos/{task_id}`。固定 15 秒。十个模型分别锁定 480/720/1080 和横竖比例：

`sd2.0-480fast-ad渠道16x9`、`sd2.0-480fast-ad渠道9x16`、`sd2.0-480满血-ad渠道16x9`、`sd2.0-480满血-ad渠道9x16`、`sd2.0-720fast-ad渠道16x9`、`sd2.0-720fast-ad渠道9x16`、`sd2.0-720满血-ad渠道16x9`、`sd2.0-720满血-ad渠道9x16`、`sd2.0-1080满血-ad渠道16x9`、`sd2.0-1080满血-ad渠道9x16`。

```json
{
  "model": "sd2.0-720满血-ad渠道16x9",
  "prompt": "产品广告，镜头流畅",
  "input": {
    "prompt": "产品广告，镜头流畅",
    "media": [
      {"type": "reference_image", "url": "https://cdn.example.com/first.jpg"},
      {"type": "reference_image", "url": "https://cdn.example.com/last.jpg"},
      {"type": "reference_video", "url": "https://cdn.example.com/motion.mp4"},
      {"type": "reference_audio", "url": "https://cdn.example.com/music.mp3"}
    ]
  },
  "seconds": "15",
  "size": "1280x720"
}
```

图片、视频、音频通过同一个 `input.media` 数组表达，顺序保持。插件最多传图9、视频3、音频3；不使用 `images`、`input_reference` 或 `reference_images`。

### 9. sd-满血不卡脸-933（按秒+按次）

**单图/文生接口：** `POST /v1/videos`、`GET /v1/videos/{task_id}`。  
**首尾/多参考接口：** `POST /v1/video/generations`、`GET /v1/video/generations/{task_id}`。

所有变体：`sd-480满血-933（按次）`、`sd-720满血-933（按次）`、`sd-480满血-933（按秒）`、`sd-720满血-933（按秒）`。时长 4-15，比例支持 `16:9`、`4:3`、`3:4`、`1:1`、`21:9`、`9:16`；图9、视频3、音频3。

**单首帧：**

```json
{
  "model": "sd-720满血-933（按秒）",
  "prompt": "人物自然转身",
  "seconds": 6,
  "size": "720p",
  "aspect_ratio": "9:16",
  "input_reference": {"image_url": "https://cdn.example.com/first.jpg"}
}
```

**首尾/多参考：**

```json
{
  "model": "sd-720满血-933（按秒）",
  "prompt": "从图1连续过渡至图2",
  "duration": 6,
  "resolution": "720p",
  "aspect_ratio": "9:16",
  "image_references": ["https://cdn.example.com/first.jpg", "https://cdn.example.com/last.jpg"],
  "video_references": ["https://cdn.example.com/motion.mp4"],
  "audio_references": ["https://cdn.example.com/music.mp3"]
}
```

文生不传 `input_reference`；多参考不能误请求 `/v1/videos/generations`，正确路径没有 `videos` 的复数。

### 10. sd轮换渠道不卡脸（动态调价）

**接口：** `POST /v1/videos`，查询 `GET /v1/videos/{task_id}`。固定 `resolution:"720p"`，比例 `16:9` 或 `9:16`。当前实现是轮换专用字段，**不是**通用 `image/images/video/videos/audio/audios` 协议。

| 变体 | 时长 |
|---|---|
| `sd-720fast-不卡脸（按次）`、`sd-720满血-较慢（按次）`、`sd-720满血-不卡脸（按次）` | 固定 15 |
| `sd-2.5-轮换渠道（按次）` | 4-15 |
| `sd-720fast（按秒）`、`sd-720满血（按秒）` | 4-15 |
| `sd-2.5-轮换渠道（按秒）` | 4-29 |

2.5 两变体限图30、视频10、音频10；其余限图9、视频3、音频3。

```json
{
  "model": "sd-2.5-轮换渠道（按秒）",
  "prompt": "首帧人物动作自然，参考素材节奏",
  "seconds": "12",
  "aspect_ratio": "16:9",
  "resolution": "720p",
  "first_frame_url": "https://cdn.example.com/first.jpg",
  "last_frame_url": "https://cdn.example.com/last.jpg",
  "reference_image_urls": ["https://cdn.example.com/ref-1.jpg"],
  "reference_videos": ["https://cdn.example.com/motion.mp4"],
  "reference_audios": ["https://cdn.example.com/music.mp3"]
}
```

文生不带任意素材字段。首帧仅 `first_frame_url`；首尾帧仅首尾两个字段；多参考仅 `reference_image_urls`，可附加视频音频。首帧+参考图由首帧字段加参考图数组表达。

### 11. sd-720满血-900（不售后）

**接口：** `POST /v1/videos`，查询 `GET /v1/videos/{task_id}`。仅支持多参考图，1-9 图，固定 15 秒、720p，比例仅 `16:9` / `9:16`。不支持文生、首帧、首尾、视频参考、音频参考。

```json
{
  "model": "sd-720满血-900（不售后）",
  "prompt": "参考三张人物图保持脸型和服装",
  "duration": "15",
  "aspect_ratio": "16:9",
  "resolution": "720p",
  "reference_images": [
    {"url": "https://cdn.example.com/ref-1.jpg"},
    {"url": "https://cdn.example.com/ref-2.jpg"},
    {"url": "https://cdn.example.com/ref-3.jpg"}
  ]
}
```

---

## 惊喜渠道

### 12. SD 2.0 惊喜渠道（卡脸 / 不卡脸）

两个分支协议相同，只是变体集合不同。

| 分支 | `model` 变体 |
|---|---|
| sd-2.0惊喜渠道（卡脸） | `sd-2.0-480满血（卡脸）惊喜渠道`、`sd-2.0-480fast（卡脸）惊喜渠道`、`sd-2.0-720满血（卡脸）惊喜渠道`、`sd-2.0-720fast（卡脸）惊喜渠道`、`sd-2.0-1080满血（卡脸）惊喜渠道` |
| sd-2.0惊喜渠道（不卡脸） | `sd-2.0-720满血（不卡脸）惊喜渠道`、`sd-2.0-480满血（不卡脸）惊喜渠道` |

时长 4-15；比例为 `16:9`、`4:3`、`3:4`、`1:1`、`21:9`、`9:16`；图9、视频3、音频3。分辨率由变体锁定。

**文生与单首帧：**

```http
POST /v1/videos
```

```json
{
  "model": "sd-2.0-720满血（不卡脸）惊喜渠道",
  "prompt": "镜头缓慢推进",
  "seconds": 8,
  "size": "720p",
  "aspect_ratio": "16:9",
  "input_reference": {"image_url": "https://cdn.example.com/first.jpg"}
}
```

文生时移除 `input_reference`。**只能一张**图片，且对象必须是 `input_reference.image_url`，不能将 URL 直接写成字符串。

**首尾、多参考、首帧+参考：**

```http
POST /v1/video/generations
```

```json
{
  "model": "sd-2.0-480fast（卡脸）惊喜渠道",
  "prompt": "首尾连续，参考运镜和音乐节奏",
  "duration": 6,
  "resolution": "480p",
  "aspect_ratio": "16:9",
  "image_references": ["https://cdn.example.com/first.jpg", "https://cdn.example.com/last.jpg"],
  "video_references": ["https://cdn.example.com/motion.mp4"],
  "audio_references": ["https://cdn.example.com/music.mp3"]
}
```

对应查询必须是 `GET /v1/video/generations/{task_id}`。首尾帧的位置由 `image_references[0]`（首）和 `[1]`（尾）决定；插件不会添加不存在的 `last_image_url` 字段。

### 13. 快乐马1.1惊喜渠道（不卡脸）

**接口：** `POST /v1/videos`，查询 `GET /v1/videos/{task_id}`。变体/模型 `快乐马1.1（不卡脸）惊喜渠道`，时长4-15，分辨率720p或1080p，比例 `16:9`、`9:16`、`1:1`、`4:3`、`3:4`、`21:9`。只支持文生和单首帧。

```json
{
  "model": "快乐马1.1（不卡脸）惊喜渠道",
  "prompt": "让人物自然微笑并向前走",
  "seconds": 8,
  "size": "720p",
  "aspect_ratio": "9:16",
  "n": 1,
  "input_reference": {"image_url": "https://cdn.example.com/first.png"}
}
```

文生移除 `input_reference`。不能传首尾帧、多图、视频或音频。

### 14. 可灵-3.0-omni惊喜渠道（不卡脸）

模型 `可灵-3.0-omni（不卡脸）惊喜渠道`，时长4-15、分辨率720p/1080p、六种标准比例。图片最多5，视频3，音频3。

**文生/首帧：** `POST /v1/videos`，查询同路径。

```json
{
  "model": "可灵-3.0-omni（不卡脸）惊喜渠道",
  "prompt": "人物朝镜头走来",
  "seconds": 8,
  "size": "720p",
  "aspect_ratio": "16:9",
  "n": 1,
  "input_reference": {"image_url": "https://cdn.example.com/first.jpg"}
}
```

**多参考：** `POST /v1/video/generations`，查询同路径。

```json
{
  "model": "可灵-3.0-omni（不卡脸）惊喜渠道",
  "prompt": "参考图中角色、视频运镜和音频节奏",
  "duration": 10,
  "resolution": "720p",
  "aspect_ratio": "16:9",
  "n": 1,
  "images": ["https://cdn.example.com/a.jpg", "https://cdn.example.com/b.jpg"],
  "video_references": ["https://cdn.example.com/motion.mp4"],
  "audio_references": ["https://cdn.example.com/music.mp3"]
}
```

注意本分支多图字段是 `images`，不是 SD 惊喜的 `image_references`。

---

## 扩展渠道

### 15. omni

**接口：** `POST /v1/videos`，查询 `GET /v1/videos/{task_id}`。固定720p、固定10秒，只允许 `16:9`、`9:16`。

| 变体 | 能力 |
|---|---|
| `omni-fast-视频生成（无水印）`、`omni-fast-视频生成（带水印）` | 文生、首帧、首尾、多参考图 |
| `omni-fast-视频编辑（无水印）`、`omni-fast-视频编辑（带水印）` | 视频编辑，1-2 个视频 |

```json
{
  "model": "omni-fast-视频生成（无水印）",
  "prompt": "从首帧自然变换到尾帧",
  "aspect_ratio": "16:9",
  "seconds": "10",
  "first_image_url": "https://cdn.example.com/first.jpg",
  "last_image_url": "https://cdn.example.com/last.jpg"
}
```

首帧只用 `first_image_url`；多参考图用 `images:[...]`，最多5张；文生无素材字段。编辑分支不用图片，而是一条 `video_url` 或两条 `videos:[...]`。

### 16. veo视频生成

**接口：** `POST /v1/videos`，查询 `GET /v1/videos/{task_id}`。模型固定 `veo视频生成`，分辨率720p，时长仅4、6、8，比例 `16:9`、`9:16`、`1:1`。

```json
{
  "model": "veo视频生成",
  "prompt": "首尾画面自然连续",
  "aspect_ratio": "16:9",
  "resolution": "720p",
  "duration": 6,
  "generate_audio": true,
  "image_urls": ["https://cdn.example.com/first.jpg", "https://cdn.example.com/last.jpg"]
}
```

首帧 `image_urls` 仅含1张；首尾帧含2张；文生省略该字段。不支持多参考、视频参考或音频参考。

---

## 字段速查和错误排查

### 不同分支图片字段不可混用

| 分支协议 | 单图 | 多图/参考图 |
|---|---|---|
| SD 惊喜 / 933 | `input_reference:{image_url}` | `image_references:[url]` |
| 可灵 Omni 惊喜 | `input_reference:{image_url}` | `images:[url]` |
| 火山 | `content` 中 `image_url:{url}` + `role` | 同左 |
| Minimax H3 | `reference_images:[{url,role}]` | 同左 |
| 轮换 SD | `first_frame_url` | `reference_image_urls:[url]` |
| SD2 全系列 / SD2.5 | `images:[url]` | 同左 |
| GROK 1.5 Preview | `reference_images:[{url:data-url}]` | 同左 |
| GROK 1.0 | `image:data-url` | `reference_images:[data-url]` |
| Horse 官方 | `image_url:data-url` | `reference_images:[data-url]` |

### HTTP 错误含义

| 状态码 | 说明与处理 |
|---|---|
| 400 | 字段、模型、时长、分辨率、素材数量或模式组合不合法。先核对本文件对应分支的请求体。 |
| 401 / 403 | API Key 无效、无权限或账户未开通。 |
| 404 | 路径错误。尤其多参考是 `/v1/video/generations`，不是 `/v1/videos/generations`。 |
| 409 | 幂等键冲突或相同任务仍在处理。 |
| 413 | JSON/图片/视频/音频过大。 |
| 422 | JSON 类型错误，例如 `images[0]` 要对象却传了字符串。 |
| 429 | 并发或频率限制。 |
| 5xx | 中转或上游服务暂时不可用；保留任务 ID 后再查询。 |

### 常见问题

1. **创建成功但界面“未知错误”**：通常是中转响应的任务 ID 或错误字段不在兼容键中。应保留创建响应原文、HTTP 状态和任务 ID；不能把余额不足、配额不足的上游文本丢弃。
2. **首尾帧没有生效**：确认模式确实选择“首尾帧”，提供了两张原图，且使用对应分支的专用字段。火山和 H3 要写角色；SD 惊喜通过多参考路径的图片顺序表达。
3. **视频已完成仍在轮询**：查询响应必须给出可识别成功状态或成片 URL。建议返回 `status:"completed"` 与 `video_url`；若没有 URL，支持的分支会下载 `/content` 回退地址。
4. **生成了错误分辨率**：对“模型锁分辨率”的分支，不要试图用 UI 的 `resolution` 覆盖模型名。火山、H3、SD2.5 和部分 SD 变体由所选模型决定。
5. **素材没被上游读取**：确认上游可访问素材 URL，且字段名与本分支一致；从本机路径生成的 `data:` 与公网 URL 不可随意互换。

---

## 给中转开发者的最小响应建议

创建响应至少返回以下任一结构：

```json
{"id":"task_xxx","status":"pending"}
```

查询成功时建议统一返回：

```json
{
  "id": "task_xxx",
  "status": "completed",
  "progress": 100,
  "video_url": "https://cdn.example.com/task_xxx.mp4"
}
```

失败时返回可直接展示的原因，避免只返回空对象：

```json
{
  "id": "task_xxx",
  "status": "failed",
  "error": {"message": "余额不足或模型未开通"}
}
```

这能让插件准确结束轮询、下载完整成片，并向用户呈现实际失败原因。
