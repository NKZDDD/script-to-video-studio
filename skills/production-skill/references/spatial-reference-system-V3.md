# 空间参考系统V3（历史索引）

> **LEGACY / 备查（V6.8 标记）**：这里只保留历史索引，不含旧版完整正文，不作为当前任务的规则来源。

现行空间分级、合法位移与必要覆盖见asset-spatial-ledger；场景丰富度与复用见asset-first-pipeline；ABC、区域派生与视频空间权责见abc-handoff-board；站位声明与七段式见model-adapters。

空间复杂度依据实际取景和交互判断，不按固定人数启用。站位声明按需要写清关键关系，不强制一行或逐人固定句式。允许可理解的越轴及合法时空省略；内部顶视图、坐标和调度记录不进入视频提示词正文，也不自动成为视频参考。
