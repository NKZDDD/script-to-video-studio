# 本分支：`v34-cinematic`

把 **AI电影级短剧生产 Skill** 编译成程序，当前跟到 **V5.6**。
和 `main` 上的 V6.1 十二环节是**两套生产体系**，不是新旧版本关系。

分支名还叫 `v34-` 是历史原因，没改 —— 改名要动远程和所有本地引用，
不值得。**看这一节判断跟到哪一版，别看分支名。**

## 零之前、最重要的一条：什么是全剧级，什么是逐集级

```
GLOBAL  n0 冻结 → n1 真相 → n2 人物世界 → n3 叙事结构
        → n4 资产表 → n4b 资产提示词 → n5 空间主表 → n6 连续性总账
              ↓ 全剧一次做完，串行
EP      n7 导演 → n8 CVS → n9 镜头 → n10 SEG 装箱 → n14 审计
              ↓ 4 集并行
SEG     n11 SCSTATE → n12 故事板 → n13 视频
```

**逐集层不许有任何跨集共享的活。** 这不是风格问题，是并发能不能成立的地基：

- V5.6 要求「维护**唯一** Continuity Ledger」，`LONG_TERM` 定义是「跨 Episode」。
  账本做成逐集的，第 5 集就看不到第 1 集留下的永久伤疤 —— 而且不报错。
- 但「第 5 集要看到第 1 集」和「4 集并排跑」天然冲突。
  把全剧级的活全部前移，逐集层就没有共享状态了，并发才安全。

踩过的两个坑都是这一条的推论：
资产提示词被 40 集各写一遍、`C001_PROMPT.txt` 互相覆盖 ——
根源都是「全剧级的活拆到逐集去做」。挪上去之后那套去重和锁**整个删掉了**。

改这一层之前先看 `tests/test_v34_episode_concurrency.py`，
它钉的就是这张图。

## 零、V3.4 → V5.6 做了什么

前三块是「已有环节里加字段和约束」，`STAGES` 没动。
第四块**动了结构** —— 四个环节从逐集改成全剧级，产物路径跟着变，
所以老项目要走一次迁移（`migrate_v56.py`，页面上会提示）。

| V5.6 新增 | 落在哪 |
|---|---|
| 六字段 `Compact Reference Identity Map` | `produce.check_identity_map()`、诊断码 `REF_MAP_INCOMPLETE`、n4b/n11/n12/n13 模板 |
| World Position State（无事件不许换位） | `gates_v34.position_gate()`、n5/n6/n7/n8/n9/n11/n12 模板 |
| 视频参考最小化 | n13 模板、`{{REF_LIMIT}}` 占位符 |
| **叙事/资产/空间/总账改成全剧级** | 环节表 scope、5 份模板、`migrate_v56.py` |

**已知没做完的一件**：V5.6 说超过 5 张参考图要逐张写
`Unique Missing Authority`，模板里要求了，但**程序没有校验**这一条 ——
只在提示词里说，模型漏写不会被拦。想补的话在 `check_identity_map`
旁边加一道，需要先能拿到「这一条实际几张参考图」。

## 一、不合并，但要单向同步

| | 怎么处理 |
|---|---|
| 体系改动（环节表、模板、`build_tasks`、校验） | **两个方向都不合。** 各自演进 |
| 引擎修复（服务商、编码、打包、并发、诊断） | **`main` → 本分支单向 cherry-pick** |

第二条不做的话，这条分支会**只累积「缺失」**：main 修的每个引擎 bug
（参考图静默丢图、GBK 解码、孤儿进程、打包漏服务商）它一个都吃不到，
最后你会在这边把同样的坑重新踩一遍。

同步方法：

```bash
git switch v34-cinematic
git log --oneline main --not HEAD -- core/produce.py core/providers/ core/executor.py \
    core/apiutil.py core/llm.py core/uploader.py core/probe.py core/store.py \
    core/paths.py core/ledger.py core/diagnose.py server/app.py 打包exe.py
git cherry-pick <那几个提交>
```

## 二、哪些文件属于哪一层

这条边界是 `main` 上 `19891ae` 专门拆出来的，**别再把两类混回同一个文件**，
混了之后 cherry-pick 每次都要手工解冲突，几次之后就没人同步了。

**引擎层（两边共用，本分支尽量不改）**

```
core/produce.py     出图出片执行：参考图形式、依赖分层、调服务商、量比例
core/providers/     10 家服务商 + 插件加载
core/executor.py    并发闸门、换家重试
core/apiutil.py     HTTP 重试、参考图编码
core/llm.py         LLM 客户端、流式、JSON 重试
core/uploader.py    对象存储
core/probe.py       尺寸探测
core/store.py       原子写、项目目录
core/paths.py       程序/数据目录分离
core/ledger.py      用量记账
core/diagnose.py    诊断目录（新体系要加自己的错误码，加在末尾，别改已有的）
core/prompts.py     三层模板机制
core/promptfile.py  提示词就地改
server/app.py       HTTP 服务（新体系要加接口，加在末尾）
打包exe.py           打包 + 自检
```

**体系层（本分支重写的就是这些）**

```
core/stages.py      环节表 STAGES / _LLM_SPEC / 模板填充 / build_tasks / 各种校验
core/pipeline.py    环节编排顺序
core/episodes.py    切集、段数
core/explorer.py    资产库与按段看视图
prompts/            全部提示词模板
web/index.html      流程页（生产页/设置/产物/任务明细基本可沿用）
```

## 三、写测试注意

`produce.py` 内部在**自己的命名空间**解析 `build_provider` / `_ratio_warn`，
所以假 provider 要 patch `core.produce.build_provider`，
patch `core.stages` 上的同名属性**不生效**（这条踩过）。

## 四、V3.4 里唯一明确不做的一条

**Geometry Proxy 出图**（第 6 章第 18 条自检）。

它要解决的是：同一个场景从三个以上机位看，出来的图**在几何上拼不拼得拢**
（门在不在同一位置、墙角对不对得上、比例一致不一致）。

不做的理由不是「暂时没空」，是**做了更糟**：出图模型做不出可靠的几何代理，
硬出一张轴测图或平面图，会得到一个**看起来权威、实际上不准**的参照 ——
下游照着它走，错得比没有它更隐蔽。V3.4 自己也把「独立 AI 多视角图」
列为不能通过空间冻结的那一档。

**注意别把它和「人物空间关系」混为一谈**（这一点最初想岔过）：

    多人之间的位置关系  ← 坐标合同已经解决，不需要 Geometry Proxy
    多机位的几何可拼合  ← 这才是 Geometry Proxy 的用途

坐标那条链是通的：

```
n5  锚点 xyz、区域、通路
 ↓
n7  每个人物 root_xyz + 朝向角度
 ↓
n8  relational_blocking：C001 → C005，距离 1.5m，面对面，同高，互看
 ↓
n9  机位坐标去投影，算出这一镜里谁在画面左、谁在右
```

换机位只改 `screen_direction`，坐标和相对关系不动 —— 这正是
「病床在画面左侧」那种写法解决不了的问题。

`n5` 停在 V3.4 空间四档里的**第三档（纯文字坐标合同）**，已知代价写在模板里：
多机位不保证严格可拼合，靠 `landmarks` 对齐。真遇到「拼不拢」再评估上 3D 工具，
那时候要的是真块模，不是让出图模型画一张假的。
