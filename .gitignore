# 密钥与本地配置（绝不提交）
config.json
# 外挂服务商目录。这台机器上数据目录**就是仓库目录**（config.json 在这儿，
# 走的是 paths.py 的老装法分支），所以往 providers/ 丢一个插件就会污染
# git status —— 和 config.json 同一类，都是运行时的东西，不是源码。
#
# ⚠ **必须带开头那个斜杠。** 不带的话 `providers/` 匹配**任何层级**叫这个名字
# 的目录，`core/providers/`（内置服务商）也被罩住 —— 已跟踪的 19 个文件不受
# 影响（gitignore 只管未跟踪的），但**以后新加的内置服务商会静默不进仓库**：
# `git add` 被拒、`git status` 里看不见，本机跑得好好的，别人 clone 下来少一家。
# 实遇 2026-09-08：云绘从插件转内置时 `git add` 被拒才发现，
# 而这行注释当时写的正是「内置服务商在 core/providers/，不受这条影响」。
/providers/
legacy/config.legacy.json
config.local.*
*.key
.env
.env.*
secrets.*

# 生产产物（体积大，另行备份）
projects/
output/
outputs/

# 打包产物（140 MB 的 exe，别进仓库；要发版就传 Release）
dist/
build/
*.spec
# 打包依赖（pip --target 装的，沙箱不让写全局 site-packages 时的本地安装位）
.deps/

# Python
__pycache__/
*.py[cod]
.venv/
venv/
env/
*.egg-info/

# 系统 / 编辑器
.DS_Store
Thumbs.db
desktop.ini
.vscode/
.idea/
*.swp
*~
*.log
*.tmp

# 剧本原稿不入库：是客户资产，且体量大、变动频繁
*.docx
*.doc
*.pdf
*.txt
!requirements.txt
# 自带的字幕样式（ASS V4+ 块）**必须进仓库** —— 打包脚本会把这个目录
# --add-data 打进 exe。被 *.txt 排掉的话，仓库里没有它，换台机器
# 重新 clone 打出来的包就少这四份样式，而缺样式不报错：
# 字幕照出，只是用 videocaptioner 的默认样子。
!字幕样式/*.txt

# 用户自己上传的字幕样式。源码方式跑的时候数据目录就是仓库根，
# 它会落在这儿 —— 那是**这台机器上这个人的**东西，不进仓库。
# （.txt 已经被上面的 *.txt 挡住了，.json 没有，所以整个目录点名排掉。）
字幕样式-自定义/

# 本地随包带的 ffmpeg.exe(87MB)。二进制进了 git 就永远在里面 ——
# 删掉也只是新提交里没有,历史里还占着,以后每次 clone 都要拖一遍。
bin/

# 打包时的模型清单快照 —— 实拉结果，天天变，不进仓库。
# 打包前用 tools/刷新模型快照.py 生成；打包脚本发现它就 --add-data 带进去。
model-snapshot.json
