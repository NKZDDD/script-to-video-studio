# 逻辑与执行框架

本文件由代码导出后整理，不是设计意图的复述。每条都能在代码里找到对应位置。
用途：和 `skills/script-to-video-prompts-v2/`（skill）逐条比对，找偏差。

---

## 0. 一句话

一个项目 = 一部剧。丢进整部剧本 → 环节1 读全篇并切集 → 每集独立走环节2-8（可并行）
→ 按选定集出图出片 → 逐集拼成片。**磁盘产物是唯一真相，"继续"就是再点一次同一个按钮。**

---

## 1. 环节表

| ID | 环节 | 名称 | 执行 | 范围 | 调用粒度 | 产物 |
|---|---|---|---|---|---|---|
| s1 | 1 | 整剧全局解析（含切集） | LLM | **全剧一次** | 整剧 1 次 | `01_剧本与分段/s1_global.json` |
| s2 | 2 | 节奏驱动段落划分 | LLM | 逐集 | 每集 1 次 | `01_剧本与分段/EPxx/s2_segments.json` |
| s3 | 3 | 状态时间线管理 | LLM | 逐集 | 每集 1 次 | `…/EPxx/s3_states.json` |
| s4 | 4 | 资产提取与生产判断 | LLM | 逐集 | 每集 1 次 | `…/EPxx/s4_assets.json` |
| s5 | 5 | 资产生产提示词编译 | LLM | 逐集 | 每集 1 次 | `…/EPxx/s5_asset_prompts.json` |
| s5b | 5 | 资产图生产（出图） | 出图 | 按选定集 | 按任务 | `02_固定资产/<类别>/<资产ID>.png` |
| s6 | 6 | 段落资产绑定 | LLM | 逐集 | 每集 1 次 | `…/EPxx/s6_binding.json` |
| s7 | 7 | 高密度正式分镜 | LLM | 逐集 | **每段 1 次** | `…/EPxx/s7_shots.json` |
| s8 | 8 | 故事板与视频提示词编译 | LLM | 逐集 | **每段 1 次** | `…/EPxx/s8_compile.json` |
| s9 | 9 | 故事板生成与固定 | 出图 | 按选定集 | 按任务 | `04_故事板/<code>_<EP>_<SEG>_STORYBOARD_V01_FIXED.png` |
| s10 | 10 | 视频执行生成 | 出片 | 按选定集 | 按任务 | `05_分段视频/<code>_<EP>_<SEG>_VIDEO_V01_FIXED.mp4` |
| s11 | 11 | 结果检查清单（人工） | 本地 | 按选定集 | 一次 | `01_剧本与分段/s11_review.json` |
| s12 | 12 | 排序拼接与交付 | 本地 | 按选定集 | 逐集 | `06_成片/<code>_<EP>_MASTER_V01_FIXED.mp4` |

代码位置：`core/stages.py` 的 `STAGES`、`SERIES_STAGES`、`_LLM_SPEC`。

---

## 2. 数据流（依赖关系）

```
整部剧本
  │
  ├─▶ s1 全剧解析 + 切集 ──────────────────────────┐
  │      产出：人物表 C001-Cnn / 场景 Snn / 道具 Pnn / 视觉基调 /
  │            伏笔 / 不可更改事实 / 长期状态 / 40 条集边界锚点
  │                                                 │
  ▼（按锚点切出每集正文，切割由代码做，不由模型做）      │
每集：                                              │
  s2 切段（本集正文 + 全局）◀──────────────────────────┘
   │ 产出 N 段：进入状态 / 核心过程 / 核心转折 / 退出状态 / 下一段锚点
   ▼
  s3 状态时间线（段落表 + 全局）
   │ 产出：timeline / segment_states / continuity_check（逐对校验相邻段）
   │        + axis_convention（整集共用的 180° 轴线与左右归属）
   ▼
  s4 资产提取（全局 + 段落 + 状态 + 【前面几集已建好的资产】）
   │ 产出：本集用到的资产（沿用已有编号，只新建真新的）
   ├──────────────▶ s5 资产提示词（旁路，见 §4）──▶ s5b 出图
   ▼
  s6 段落绑定（段落 + 状态 + 资产表）
   │ 产出：每段用哪 ≤5 张参考图、各控制什么、排除哪些
   ▼
  s7 正式分镜（段落 + 状态 + 绑定 + 轴线约定）   ← 按段并行
   │ 产出：每段 5-8 镜头 + 关键帧采样 + 硬切锚点
   ▼
  s8 编译（基调 + 段落 + 状态 + 资产 + 绑定 + 分镜）  ← 按段并行
     产出：每段一份故事板提示词 + 一份视频提示词（落成 txt）
        │
        ▼
     tasks.json ──▶ s9 故事板 ──▶ s10 视频 ──▶ s11 复核清单 ──▶ s12 拼接
```

注意 s8 的依赖里**没有 s5** —— 这是 s5 能走旁路的依据。

---

## 3. 每个 LLM 环节的输入输出契约

| ID | 模板 | 依赖产物 | 输入区 | 必需输出字段 |
|---|---|---|---|---|
| s1 | `s1_global.md` | 无 | 【项目参数】【完整剧本】 | `project_name` `characters[]` `visual_tone` |
| s2 | `s2_segments.md` | s1 | 【全局解析结果】【本集剧本】 | `segments[]` `.id` `.exit_state` |
| s3 | `s3_states.md` | s1, s2 | 【本集段落表】【全局解析】 | `segment_states[]` |
| s4 | `s4_assets.md` | s1, s2, s3 | 【全局解析】【已有资产】【本集段落表】【本集状态时间线】 | `assets[]` `.asset_id` |
| s5 | `s5_asset_prompts.md` | s1, s4 | 【视觉基调压缩版】【待生产资产清单】 | `asset_prompts[]` `.prompt` |
| s6 | `s6_binding.md` | s2, s3, s4 | 【本集段落表】【本集状态时间线】【资产清单】 | `bindings[]` |
| s7 | `s7_shots.md` | s2, s3, s6 | 【本集轴线约定】【本集段落表】【本集状态时间线】【本集资产绑定】 | `shots[]` |
| s8 | `s8_compile.md` | s1, s2, s3, s4, s6, s7 | 【视觉基调压缩版】【本集段落表】【本集状态时间线】【资产清单】【本集资产绑定】【正式分镜】 | `compiled[]` `.storyboard_prompt` `.video_prompt` |

**编排逻辑在代码里，不在模型手里**：模板是固定的，占位符由代码填，输出按 `必需字段`
校验，缺字段就带反馈重试。模型只负责内容，不负责决定下一步做什么。

---

## 4. 调度

### 并行三层

| 层 | 参数 | 默认 | 说明 |
|---|---|---|---|
| 集与集 | `llm_episodes` | 4 | 同时处理几集 |
| 段与段 | `llm_segments` | 4 | 环节7、8 内部同时做几段 |
| 在途总闸 | `llm_concurrency` | 6 | LLM 在途请求上限，真正的天花板 |
| 出图出片 | `concurrency` + 服务商配额 + 全局闸 | 3 | 另一道闸，和 LLM 那道互不相干 |

### 必须排队的三处

1. **环节1** —— 全剧一次，所有集都等它（它才知道有几集）
2. **同集内 s2 → s3 → s4 → s6 → s7 → s8** —— 每步吃上一步的产物
3. **跨集的环节4，按集号顺序** —— 它的输入含「前面几集已建好的资产」，先出现的定义
   优先。若 EP05 抢在 EP03 前面做环节4，两集会各自给同一个新对象编号，同一个编号
   指两个东西，出图张冠李戴。实现：每集一个 Event，前一集的环节4 结束才放行（失败
   也放行，否则后面全卡死）。

### 旁路

**环节5** 在环节4 一出来就丢到旁路，与 6/7/8 并行；它失败只影响那几个资产的出图，
本集的分镜和编译照常跑完（`plan()` 里打 `side` + `soft` 标记）。

### 不能拆的两处，以及为什么

- **环节3 不能按段拆**：它的产物含 `continuity_check`，逐对校验相邻段的 exit/entry
  （EP01 12 段 → 11 对）。这是 skill 规定的段间衔接**唯一**机制，拆开就没有了。
- **环节6 没有按段拆**（虽然它是按段的）：它的活是「从全剧资产表里挑 ≤5 张」，每段
  都得看到整张表。按段拆要把资产表重发 N 遍 —— EP01 实测输入会从约 1.3 万 token
  涨到约 7.6 万，只换 2.5 分钟。不划算，所以保持整集一次。

### 环节7 按段拆的前提

实测 EP01 整集一次跑时，各段的 `axis_note` 互相引用（「沿同一玻璃门轴线」「轴线保持
不变」），12 段共用一套左右归属。这个一致性是"一次看完全部段落"换来的。所以按段拆
之前，先让**环节3 输出整集共用的 `axis_convention`**（主轴线怎么建立、左右归属、
前后景归属、进出方向、要换轴的段落显式声明），环节7 各段引用它，不再各自定轴。

老项目的环节3 产物没有这个字段 → 环节7 **自动回退到整集一次跑**，产物照样能用。

---

## 4.5 部署：程序目录 vs 数据目录

**更新程序 = 覆盖程序目录。所以配置和产物一律不放在里面。**

| | 放什么 | 更新程序时 |
|---|---|---|
| 程序目录 `script-to-video-studio/` | 代码、提示词模板、页面 | **整个被覆盖** |
| 数据目录 | `config.json`（各家 key、R2 凭证、优先级链、计价表） | 不碰 |
| 产物目录 `projects/` | 剧本、资产图、故事板、视频（几十 GB） | 不碰 |

数据目录按这个顺序定，先命中的算（`core/paths.py`）：

```
1. python run.py --data D:\stv-data
2. 环境变量 STV_DATA_DIR
3. 程序目录里已经有 config.json —— 老装法，原地不动别乱搬
   （启动时会提醒：这个位置覆盖程序就丢；设置页有一键搬出）
4. %LOCALAPPDATA%\script-to-video-studio
```

启动时必打这四行，换机器时比任何文档都管用：

```
  程序目录  ...\script-to-video-studio
  数据目录  D:\stv-data   （--data 参数）
  配置文件  D:\stv-data\config.json
  产物目录  D:\stv-data\projects
```

**换到另一台电脑**：拷程序目录（或 `git clone`）→ `python run.py --data D:\stv-data`。
两台机器各有一份配置和产物，互不影响。

**两台机器共用同一批产物**：把产物目录指到同一个共享盘。「做过没有」只看磁盘产物，
所以另一台能直接接着跑。**但别让两台同时跑同一个项目** —— 会写同一批文件。

---

## 5. 磁盘布局 = 唯一真相

```
项目根/
├── 00_项目说明/project.json              项目参数、code、集清单
├── 01_剧本与分段/
│   ├── 原始剧本.txt
│   ├── s1_global.json                   全剧一份
│   ├── episodes.json                    集边界（锚点、字数、进出状态）
│   ├── EP01/ s2_segments s3_states s4_assets s5_asset_prompts
│   │         s6_binding s7_shots s8_compile   （各 .json）
│   ├── EP02/ …  EP40/
│   └── s11_review.json
├── 02_固定资产/人物身份|群体|生物|场景|道具|连续状态 资产/
├── 03_提示词/资产生产|故事板|视频|定向修订 提示词/
│   └── tasks.json                        出图出片的机器可读清单
├── 04_故事板/  05_分段视频/  06_成片/
└── 07_检查与记录/
    ├── usage.jsonl                       用量账本（只追加）
    └── failures.json                     当前未解决的报错
```

### 断点续跑

- 「这一步做过没有」**只看磁盘产物**，不看任何运行记录（`pipeline._llm_done`）
- 环节7、8 **按段判断**：排了 8 段剩 4 段时，文件是存在的，但这一步没做完
- 写文件是原子的：先写 `xxx.json.<pid>.tmp` 再 `os.replace`，绝不留半个坏 JSON
- 所以：进程杀了 / 服务重启 / 换台电脑，再点一次「开始」就是继续；想重跑某步就删它的产物
- 资产图、故事板、视频**已存在就跳过不覆盖**（生成即固定）

### 资产为什么是"按整部"

三条机制，缺一不可：

1. **环节1 一次读完 40 集**定人物表（C001-Cnn 全剧统一，含 appearance 写死）
2. **环节4 的 `KNOWN_ASSETS`** 让后面的集沿用前面的编号（并发下靠按集号排队保证）
3. **资产图路径不含集号** → 同一个 `asset_id` 全剧只有一个文件，物理上不可能两张脸

而 **s6 的"按段"是领料单，不是资产**：同一个 `C001` 在 SEG01 是 Image 1、在 SEG05
可能是 Image 2，指向的始终是同一个 `C001.png`。skill 说的「每个段落重新从 Image 1
开始编号」指的是上传槽位号，不是给资产起新编号。

---

## 6. 出错策略

### 三级 + 一种

| 级别 | 判定 | 行为 |
|---|---|---|
| 整批致命 | 余额不足、密钥无效、域名连不上（`scope == batch`） | **立刻停整条流水线**，剩下的步骤**原地留 pending**（表示压根没发出去） |
| 整集卡住 | 某集某个主链环节失败 | 这一集后续环节全部 `skipped`，**其它集继续** |
| 单项失败 | 单张图 / 单段视频 / 单段分镜 / 单段编译 | 只这一项失败，同批其它照做 |
| 用户取消 | 点了「停下」 | 标 `cancelled`，不算失败、不重试、不进 failures |

`abort_with` 会连带把 `cancelled` 也置真（让在途 worker 收手），所以判断停机原因时
必须先判 `aborted`，否则余额不足会显示成「已取消」。

### 21 个诊断码，全部大白话，附「怎么办」

```
换家能解决（自动切下一家）：ACCOUNT_BANNED AUTH_INVALID MODEL_NOT_FOUND NETWORK
                            QUOTA_EXHAUSTED RATE_LIMITED REF_URL_ONLY TIMEOUT
换家也没用（不自动切）    ：CONTENT_REJECTED DISK EPISODE_REQUIRED
                            EPISODE_SPLIT_FAILED LLM_EMPTY LLM_SCHEMA_FAIL
                            PREREQ_MISSING PROMPT_INVALID REF_MISSING WRONG_RATIO
其它                      ：LLM_TRUNCATED MODEL_NEEDS_REF UNKNOWN
```

分类的道理：提示词被审核挡下、参数不合法、参考图还没生成 —— 换家也是同一个结果，
自动切只会把同一个错误重复几遍还多花钱。

### LLM 侧的错误分类

- `LLMFatal` —— 超时、输出被长度上限截断。**永不重试**（同一个提示词再来一遍也一样）
- `_Retryable` —— 429 / 5xx / 流中途断开。退避重试
- `LLMCancelled` —— 用户取消。不是失败
- `_NoStream` / `_NoStreamOptions` —— 网关不支持流式或 usage 选项，自动降级重发

---

## 7. 服务商

| id | 名称 | 能力 | 别名 |
|---|---|---|---|
| paisio | 鹤 api.paisio.online（SD2/SD3/Seedance） | 出图 + 出片 | he, pis, 派系 |
| lingganya | 灵感鸭 lingganyaapi.com | 出图 + 出片 | — |
| zeroapi | 零视工坊 zeroapi.ai-ren.cn | 出图 + 出片 | — |
| aicopy | 小裴 api.aicopy.top | 出图 + 出片 | — |
| kunji | 坤鸡 img.yunfei.best | 只出图 | — |
| octopus | 章鱼哥 | 出图 + 出片 | — |

- 出图 / 故事板 / 出片**各排一条优先级链**。首选挂了按 `should_failover()` 判断，
  该换就自动换下一家**只补没做成的那些**，面板上写清换了几次、换到了谁
- 点「开始」前 `preflight_models()` 零成本核对模型名是否真的存在
- 提示词可选参数（尺寸 / 比例 / 时长）取链上**所有家的交集**，避免选了别家不支持的值
- 生成后测量实际尺寸（`core/probe.py`，不依赖 Pillow），和期望比例不符就告警 —— 防
  "服务商静默返回了错规格的东西"
- 参考图需要公网 URL 的服务商：走 R2/S3 上传（内容哈希缓存 + 上传自检）

---

## 8. 记账

每次调用往 `07_检查与记录/usage.jsonl` 追加一行：环节 / 集 / 段 / 服务商 / 模型 /
prompt+cached+completion tokens / 秒数 / 是否为估算值。用量页按环节、按模型、总计三个
维度汇总。**有价目表才算钱，没填就只报数量 —— 绝不猜价。**

---

## 9. 量的账（EP01 实测为基准）

### 步数与调用次数

```
面板显示步数 = 1 + 7×40 + 3 + 2 = 286
真实 LLM 调用 = 1(环节1) + 5×40(环节2-6) + 段数×2(环节7、8 按段)
              EP01 有 12 段 → 单集 5 + 24 = 29 次
              全剧按平均 8 段估 ≈ 841 次
```

### 单集耗时（EP01，串行基准 48.5 分钟）

| 优化 | 单集耗时 |
|---|---|
| 原始全串行 | 48.5 分钟 |
| 环节8 段并发 4（21 → 6 分钟） | 33.5 |
| 环节5 挪出关键路径（−5.2） | 28.3 |
| 环节7 段并发 4（10.7 → 3，−7.7） | **20.6** |

### 40 集全量

约 2.5 小时。地板是**环节4 按集串行**的 40 × 3 = 120 分钟，加网关限流。
所以进一步拆段对全量帮助有限，主要收益在单集测试循环。

### 并发不改产物

跨集并行、段内并行只改调度。`test_parallel` 逐字段 diff 断言：并发跑的产物和串行跑
**逐字一致**。

---

## 10. 和 skill 的逐条比对

### 一致的

| skill 要求 | 位置 | 系统实现 |
|---|---|---|
| 先理解整部剧本，再处理单集和单段 | 01:9 | 环节1 吃整部剧本，且**只跑一次** |
| 环节1 必须提取「每集剧情范围/进入/退出状态」 | 01:13 | `episodes.json` 40 条，含 entry_state / exit_state |
| 范围标记 `full_series + canonical` | 01:33-38 | s1 输出 `scope`，实测值 `full_series + canonical` |
| appearance 写死稳定特征、供环节8 复制 | 01:24 | s1 的 `characters[].appearance`，环节8 原文引用 |
| 上一段退出状态 = 下一段进入状态，逐对核对 | s3 模板:51 / SKILL:161 | `continuity_check`，EP01 11 对全部 `consistent:true` |
| 每段 5-8 镜头、故事板 4-6 格、采样关系不是 1:1 | 03 / SKILL:163 | `SHOTS_MIN/MAX`、`FRAMES_MIN/MAX` 参数化；EP01 12 段 × 6 镜头 |
| 保持 180° 轴线、固定阵营左右 | s7 模板:16-17 | 环节3 定 `axis_convention`，环节7 各段引用（**比 skill 更强制**） |
| 每段参考图 ≤5 张、每段重新从 Image 1 编号 | 02:132 | s6 的 `reference_images` 带 `image_n`，EP01 各段 5 张 |
| 状态资产只描述差异，先复述父资产 anchors | SKILL:162 | s5 模板「状态资产额外要求」 |
| 已成功并通过检查的资产/故事板/视频立即固定 | SKILL:13 | 文件名 `_V01_FIXED`；已存在就跳过不覆盖 |
| 固定版本不得覆盖，改要建 V02 | SKILL:113 | 输出路径固定 `_V01_FIXED`，覆盖需显式操作 |
| 禁止一个错误导致整集全部重做 | SKILL:150 | 环节7/8 段级隔离，失败只补那一段 |
| 标准生产包目录 | SKILL:117-127 | `core/store.py` 的 `DIRS` 与之一致 |
| 命名 `EPxx-SEGxx` | SKILL:106 | 段 id 自带集号前缀，全系统贯穿 |
| 开工前先看做到哪一环节，从那里继续 | SKILL:129 | 磁盘产物为准的断点续跑 |
| 环节9-10 是可替换模块，执行器无关 | SKILL:71-85 | 契约是 `tasks.json` + 提示词文件 + 固定资产；服务商可插拔 |
| 不判断内容质量，只记录 | SKILL:14 / 执行器纪律 | 只测尺寸/比例这类技术项；审美一律不判 |

### 有意偏离的

| # | skill | 系统 | 为什么 |
|---|---|---|---|
| 1 | 生产包目录是 `PROJECT-001_EP01/`，一个包 = 一集 | 一个项目 = **一部剧**，集作为 `01_剧本与分段/EPxx/` 一层 | 40 集要跨集统一资产编号和人脸，一集一个包做不到 |
| 2 | 环节5 = 资产生产与固定（一个环节） | 拆成 `s5`（编提示词，LLM）+ `s5b`（出图，服务商） | 两者失败原因完全不同，要能分别重试 |
| 3 | 环节1 = 整剧全局解析 | 环节1 = 全局解析 **+ 切集** | 一部剧一个项目，切集必须在最前面；边界由模型判断、切割由代码按锚点做 |
| 4 | 环节2-8 是单集视角，对话里顺次跑 | 逐集循环 + `KNOWN_ASSETS` 回灌 + 跨集并行 | skill 没有"批量 40 集"这个场景，这一层是补的 |
| 5 | 环节7、8 在对话里各一次出完整集 | 按段调用 | 整集一次输出太长会被截断（EP01 环节8 产物 198 KB），且中途失败整批白跑。skill 自己的「禁止一个错误导致整集重做」支持这个拆法 |

### ⚠️ 真实缺口（需要你定）

| # | skill 明确要求 | 系统现状 | 后果 |
|---|---|---|---|
| **A** | **「唯一人工确认点：环节 1 结束后停一次」**，确认全局解析 + 视觉风格 + 范围标记（SKILL:131-139，01:40） | **一键跑到底不停**，环节1 完成直接往下发 | 若 s1 的人物表/基调判错，40 集全部继承。有「先看会做什么」预览，但不是强制停点 |
| **B** | 环节11 = 结果检查**与定向修订**，八层检查、定位错误层级、L2 问题生成 V02 提示词（05-review-and-deliver.md） | 只产出**人工复核清单**（`s11_review.json`：episodes / levels / rows），没有自动定向修订 | 发现问题要手改提示词 + 点「补失败」。`03_提示词/定向修订提示词/` 目录建了但没人写 |
| C | 十五条全局原则里的内容类约束（不删关键剧情、不改施动者、不用机械快进…） | 全部写进 prompt，**代码不校验** | 靠模型自觉 + 环节11 人工验收 |
| D | 十条禁止自动执行 | 同上，prompt 层 | 同上 |

A 的修法：给「一键跑到底」加一个「环节1 跑完停下等我确认」的勾选（默认开）。
B 的修法：环节11 增加一步 —— 按 rows 里标 L2 的项，读原提示词 + 问题描述，生成 V02
提示词写进 `03_提示词/定向修订提示词/`，再走一次 s9/s10。两者都没做。

---

## 11. 和 GPT 聊天记录的差异

聊天记录：`C:\tmp\chatgpt_剧本自动化2.0_v2.md`（103 KB，5301 行）

```
行 807  User      上传《张角》+「你需要全部分析，但我只需要做第一集」
行 816  ChatGPT   24,439 字符，一次回复，直接给出第八环节 · EP01 十段提示词
```

那一整块回复里出现的环节标题只有三处（`第八环节` 标题、"直接继承第七环节"、
`第八环节输出注册`）。**环节1-7 的产物一个字都没打印出来** —— GPT 在这一次回复里
把它们全做在内部推理里了。

| | GPT 单次模式 | 本系统 |
|---|---|---|
| 调用次数 | 一集 1 次 | 一集 29 次（EP01） |
| 环节1-7 的产物 | 只在对话上下文里，关掉就没了 | 全部落盘为 JSON |
| 第 37 集还是同一张脸吗 | 靠上下文回忆 | 读 `s1_global.json` 的 C001 |
| 某一步判错想只重来那一步 | 整段重跑 | 删一个 json 重跑一步 |
| 想打开检查资产表 | 没有表可看 | 20 个资产的 JSON 摆着 |
| 关机第二天继续 | 从头开始 | 磁盘为准，接着跑 |
| 规模 | 那次是 1 集 10 段 | 40 集、60,907 字正文、52 项资产 |

**多次调用买的不是质量，是可检查、可定点重跑、可跨集一致。** 它是 40 集这个规模逼
出来的，不是环节本身要求的。

另一个实测差异：同一段的提示词，GPT 那版 2,565 字符，本系统 5,609 字符（**2.2 倍**）。
膨胀在于本系统把 5 张参考图的 appearance 原文全量复制进提示词，而 GPT 只写一行
「Image 1 = 张角唯一3D角色模型参考」。本系统的做法照的是 skill 01:24 的字面
（appearance 会被环节8 直接复制进图片提示词）。**已知，暂不改** —— 用户明确表示
token 不是当前主要矛盾。

---

## 12. 已知的其它数据问题

**段数不一致**（实测，未修）：

```
EP01  1,735 字  →  12 段
EP02  1,513 字  →   4 段
```

字数差 13%，段数差 3 倍。下游后果：EP01 成片 12×15 = 180 秒，EP02 只有 60 秒，同一部
剧每集时长不一样。环节2 的提示词缺一条「按字数/时长定段数」的硬约束。

**切集本身是干净的**（已验证）：

```
81,606 = 20,647（前言/人物表）+ 60,907（40 集正文）+ 52（集间分隔）
40 个 start_anchor 全部匹配，issues = 0
原始文件 83,839 − 81,606 = 2,233 = \r 的个数（\r\n→\n），逐字对得上
```

---

## 13. 测试

`scratchpad/test_*.py` 共 20 个，其中 `test_parallel.py` 57 项断言覆盖：

- 多集重叠在跑；环节4 严格按集号且互不重叠
- 环节7、8 段内真并发；在途数不超总上限
- **并发产物与串行逐字一致**
- 资产编号跨集沿用、提示词只写一份（并发下不重复写）
- 一集挂了只跳过那一集；环节5 挂了不拖累主链
- 取消能停住且存盘完整；再点一次接着跑不重做
- 单集场景：只碰 EP01、调用数 = 1 + 5 + 段数×2、产物与全剧跑一致
- 没有轴线约定时环节7 回退整集一次
- 环节7 裁成 2/5 段后续跑只补缺的 3 段
