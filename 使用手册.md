# 使用手册

一份剧本 → 12 个环节 → 成片。本地跑，产物落在你自己的盘上。

> 这份讲**怎么用**。想知道**内部怎么运作**（环节依赖、并发调度、错误策略、
> 和 skill 的逐条对照）看 [FRAMEWORK.md](FRAMEWORK.md)。

---

## 目录

1. [装和启动](#1-装和启动)
2. [第一次配置](#2-第一次配置)
3. [跑第一部剧](#3-跑第一部剧)
4. [页面上每块是干嘛的](#4-页面上每块是干嘛的)
5. [出错了怎么办](#5-出错了怎么办)
6. [续跑与重跑](#6-续跑与重跑)
7. [并发怎么调](#7-并发怎么调)
8. [花了多少钱](#8-花了多少钱)
9. [换电脑 / 更新程序](#9-换电脑--更新程序)
10. [常见问题](#10-常见问题)

---

## 1. 装和启动

### 依赖

只有一个必需的：

```bash
pip install requests
```

三个可选的，缺了不影响主流程：

| 包 | 缺了会怎样 |
|---|---|
| `Pillow` | 参考图不压缩就转 base64 —— 请求体变大，可能超限 |
| `pypdf` | 读不了 PDF 剧本（docx / txt 不需要任何依赖） |
| `imageio-ffmpeg` | 环节12 拼不了成片（装了系统 ffmpeg 也行） |

```bash
pip install Pillow pypdf imageio-ffmpeg
```

### 启动

```bash
python run.py
```

浏览器会自动打开 `http://127.0.0.1:8770`。

启动时会先打四行路径 —— **换机器时这四行比什么文档都管用**：

```
  程序目录  D:\script-to-video-studio
  数据目录  D:\stv-data   （--data 参数）
  配置文件  D:\stv-data\config.json
  产物目录  D:\stv-data\projects
```

常用参数：

```bash
python run.py --port 8888          # 换端口
python run.py --no-browser         # 别自动开浏览器
python run.py --data D:\stv-data   # 指定数据目录（见第 9 节）
python run.py --host 0.0.0.0       # 让局域网里别的机器也能访问
```

> ⚠️ `--host 0.0.0.0` 之前想清楚：**这个服务没有登录，谁能访问就能用你的 key、
> 看你的产物、改你的配置。** 只在你信任的内网用，别对公网开。默认的 `127.0.0.1`
> 只有本机能连。

`Ctrl+C` 停止。**关掉服务不会丢任何东西** —— 一切以磁盘产物为准。

---

## 2. 第一次配置

全在「设置」页，从上往下配一遍。**配置存在 `config.json`，不进 git，不外发。**

### 2.1 服务商

每家一张卡，填 API Key（Base URL 有默认值，一般不用改）。目前接了 7 家：

| 名称 | 能做什么 | 参考图怎么给 | 备注 |
|---|---|---|---|
| 鹤 api.paisio.online | 出图 + 出片 | 链接 / data URI 都行 | SD2/SD3/Seedance，最稳的出片通道 |
| 灵感鸭 lingganyaapi.com | 出图 + 出片 | 链接 / data URI 都行 | |
| 零视工坊 zeroapi.ai-ren.cn | 出图 + 出片 | SD2 系列**只收链接** | 用 SD2 要配对象存储 |
| M86 / New API yiyun.xiaoge.uk | 出图 + 出片 | **出图只收链接**；出片两种都行 | size 填的是比例不是像素；出片固定 $1.2/次 |
| 小裴 api.aicopy.top | 出图 + 出片 | 出图只收内联 base64 | 6 条 gpt-image-2 应急通道；**出图只收 1 张参考图** |
| 坤鸡 img.yunfei.best | 只出图 | 只收本机文件字节 | multipart 接口 |
| 章鱼哥 | 出图 + 出片 | 链接 / data URI 都行 | |

**只填你真的有 key 的**。没填的家不会出现在优先级链的候选里。

> 「参考图怎么给」这一列不用你操心 —— 程序按每家的声明自动给对形式。
> 但它决定了**哪些活能排给哪家**，见下一节的警告。

### 2.2 分析引擎（环节 1-8 的 LLM）

| 字段 | 说明 |
|---|---|
| Base URL | 网关地址 |
| API Key | |
| 模型 | 比如 `gpt-5.6-sol`。旁边可以拉取模型列表 |
| **单次输出上限 tokens** | 默认 16000。**环节1 要为整部剧输出人物、场景、道具、伏笔加每一集的边界，很容易超** —— 报「话没说完就被截断」就调大到 32000 或 64000 |

### 2.3 出图出片优先级

三类活各排一条队：**资产图 / 故事板 / 分段视频**。

- 每条队可以排多家，**首选挂了自动换下一家，只补没做完的那些**
- 只有「换一家能解决」的错才切（余额不足、密钥失效、限流、没这个模型、连不上）
- 「换家也没用」的不切（提示词被审核挡下、参数不合法、参考图还没生成）—— 换家只是把同一个错误重复几遍还多花钱

**排队顺序建议**：把最稳的放首选，应急通道放后面。

> ⚠️ **哪些活不能排给哪家**（排错了会失败并自动换下一家，不会静默出错图）：
>
> | 家 | 限制 | 后果 |
> |---|---|---|
> | 小裴 | 出图只收 **1 张**参考图 | 状态资产（要父资产）、故事板（要 5 张）排给它必失败 |
> | M86 | **出图只收公网链接** | 没配对象存储时，凡是要参考图的活都失败 |
> | 零视 SD2 系列 | 只收公网链接 | 同上 |
>
> 这三种情况以前会「图照出、参考图被悄悄丢掉」—— 出来的脸不是本人，任务还标成功。
> 现在一律报错换下一家。**建议：资产图和故事板的首选排给鹤或灵感鸭，
> 小裴/M86 放后面当应急。**

### 2.4 参考图上传（对象存储，可选）

有些模型只收公网链接（零视 SD2、seedance 系）。配了 S3 兼容存储（R2 等）之后，
参考图统一走链接，请求体从几 MB 缩成一行。

填完点「测试连接」—— 它会真传一个小文件再用浏览器同样的方式取回来，确认服务商那边真能读到。

**不配也能跑**，只是那类模型用不了。

### 2.5 提示词模板（想改基础提示词时看这里）

设置页有一张「提示词模板」卡片，把**每个环节实际发给模型的原文**摊开，可以直接改。

选一个环节 → 看到全文 → 改 → 保存。旁边还有「看内置原文」（逐行差异）和「恢复内置」。

**`{{XXX}}` 是程序往里填数据的口子** —— 剧本、上一环节的产物、项目参数都从这些位置进去。
卡片上会标出：

- **必需占位符**（绿色）—— 删了会被拦住。比如环节2 的 `{{SCRIPT}}` 删了，模型就收不到剧本
- 其余可用的占位符
- **必需输出字段** —— 模型不按这个字段输出的话，程序校验不过会反复重试然后失败

**保存前会校验**，分两级：

| | 例子 | 行为 |
|---|---|---|
| **拦住** | 删了必需占位符、删了必需输出字段、模板是空的 | 不写盘，除非你二次确认强存 |
| 提醒 | 加了程序不认识的占位符、去掉了非必需的、JSON 示例解析不了 | 照样存，只是告诉你一声 |

为什么要拦：**模板改坏了不会立刻报错**，会一路跑下去，几百次调用之后才发现产出不对。

> 改写存在**数据目录**下的 `prompts/`，**不动内置那份**。随时能一键恢复；
> 程序更新、换 exe 都不会冲掉你的改写。

### 2.6 服务商插件（不改程序就加一家）

设置页的「服务商插件」卡片列出当前所有服务商、各自从哪儿来（内置 / 哪个插件文件）。

**加一家：**

1. 点「建插件目录并放一份范例」
2. 打开那个目录，把 `_示例_把我改成你的服务商.py.txt` 复制一份，照着改
3. **把文件名的 `.txt` 去掉**（半成品带着 .txt 不会被加载，免得刷一堆错误）
4. 回来点「重新扫描」

插件目录在**数据目录**下，不在程序目录里 —— 更新程序、换 exe 都不会动它。
打包成 exe 之后也能这么加，因为 exe 里的代码是只读的，而这个目录不是。

**必填**：继承 `Provider`，给 `id / name / supports`，实现 `capabilities()` 和
对应的 `generate_image` / `generate_video`。

**参考图形式一定要声明对**（`ref_mode` / `needs_url` / `accepts_url`）——
声明错了不会报错，只会让参考图被悄悄丢掉，出来的图不是同一个人，任务还标成功。
范例里有说明。

**加载失败不会拖垮程序**：写错的那个文件被红字列出来（含具体报错），其余的照常用。
声明有问题的（比如说支持视频却没实现）会黄字提醒，能用但要留意。

插件的 `id` 和内置的重名时，**插件赢** —— 这样能不改程序就替换某一家的实现，
页面上会明确告诉你「覆盖了原来来自内置的那个」。

> ⚠️ 插件是真的会被执行的 Python 代码，只放你自己写的或信得过的。

### 2.7 数据与路径

看第 9 节。**第一次用先看一眼这里有没有红框提示。**

### 2.8 同时最多跑几个

| 字段 | 默认 | 说明 |
|---|---|---|
| 总上限（所有项目合计） | 8 | 同时做几部剧也共用这个 |
| 每家服务商配额 | 鹤 6 / 灵感鸭 4 / 其余 4 | |

被服务商拦（提示发得太快）的时候，就是把这里调小。

### 2.9 默认参数

| 字段 | 默认 | 影响 |
|---|---|---|
| 单段秒数 `duration` | 15 | **视频模型一次能生成多少秒**，是硬约束。段数 = 每集秒数 ÷ 这个值 |
| 画面比例 `ratio` | 9:16 | |
| 出图尺寸 `image_size` | 1024x1536 | |
| 默认并发 `concurrency` | 3 | 出图出片的 |
| 技术重试上限 `max_retry` | 2 | |

项目参数（「流程」页）会覆盖这里的默认值，只影响那一个项目。

> **这里只放「真的要人来定」的东西。**
> 每段几个镜头（5–8）、故事板几格（4–6）、这一集多长 —— 这些**不是配置项**，
> 是给模型判断用的创作区间，由它按每一段的信息密度、每一集的剧情事件自己定。
> 数字写在提示词模板里（skill 规定的），想改去「设置 → 提示词模板」。
> 做成旋钮会误导人去「控制」本该由内容决定的东西。

### 2.10 计价（可选）

填了才显示估算金额，不填只统计用量。**绝不猜价** —— 各家计价方式差别太大，
猜一个假数字比没有更糟。

键可以是 `服务商/模型`、`模型` 或 `服务商`，由细到粗匹配。

```json
{
  "llm/gpt-5.6-sol": {"in": 2.0, "out": 8.0, "cached_in": 0.5, "per": 1000000},
  "paisio/sd2-pro-720p": {"per_call": 0.35}
}
```

---

## 3. 跑第一部剧

### 3.1 建项目

「项目」页两种方式：

- **批量建剧** —— 一次拖多个剧本文件（docx / txt / pdf），一个文件一个项目
- **单个项目 · 粘贴正文** —— 没有文件时用

建完点「打开」进「流程」页。

### 3.2 跑环节1

「一键跑到底」卡片：

1. 点 **「只测第一集」** —— 它会同时设好「分析这几集」和「只出这几集的图/片」两个框
2. 点 **「先看会做什么（不花钱）」** 核对一下
3. 点 **「开始 / 继续」**

环节1 会读**整部剧本**（这是必须的 —— 人物表、视觉基调、集边界都得看全篇），
然后切出全部集。它是全剧唯一一次全量分析。

### 3.3 ⭐ 环节1 跑完先停下来看一眼

这一步定错了后面几百步全跟着错，**值得花两分钟人工核对**。

去「这部剧有几集」卡片，点开「每集的时长和理由」：

```
集     字数   时长   段数  实际   字/分   为什么是这个时长
EP01  1735   180s   12   180s   578    5 个关键事件，雨夜弃局占大头
EP02  1513    90s    6    90s   1009   过渡集，只有 3 个事件
```

核对三件事：

| 看什么 | 不对的话 |
|---|---|
| **集数对不对** | 40 集的剧本切出 12 集 → 集边界识别错了，重跑环节1 或换更强的模型 |
| **每集时长合不合理** | 过渡集也 300 秒 → 环节1 没认真数事件 |
| **有没有红字的「字/分」** | 低于 100 = 内容撑不满会注水；高于 2000 = 塞不下 |

还要看卡片上有没有黄框的「切集时有 N 处可疑」—— 那是锚点没匹配上，那几集正文会是空的。

**每集多长是环节1 按剧情事件定的，不是套「单集分钟」。** 段数由程序算：

```
段数 = ⌈这一集的秒数 ÷ 单段秒数⌉

同一个 180 秒的 EP01：
  单段 15 秒（默认）→ 12 段
  单段 10 秒         → 18 段
  单段 20 秒         →  9 段
```

**换视频模型不用重跑环节1** —— 改「设置 → 默认参数 → 单段秒数」，段数自动跟着变。
段不可分，所以秒数会上取整到单段秒数的整数倍（要 100 秒 → 7 段 = 105 秒）。

### 3.4 往下跑

确认没问题就再点一次「开始 / 继续」。它会：

```
环节2-8（逐集，可并行）→ 出资产图 → 出故事板 → 出视频 → 复核清单 → 拼接成片
```

中间任何一步出问题都会停下来告诉你改哪里，**改完回来再点同一个按钮**就是续跑。

### 3.5 验收

「产物」页三个视图：

- **资产库** —— 这部剧有哪些固定资产、出到什么程度
- **按段看** —— 这一集每段从段落到成片走到哪一步
- **原始文件** —— 按目录直接翻盘

成片在 `06_成片/`，一集一个 mp4。

---

## 4. 页面上每块是干嘛的

### 项目页

| 卡片 | 干嘛的 |
|---|---|
| 项目列表 | 所有项目，点「打开」进去 |
| 批量建剧 | 一次拖多个剧本 = 一次建多个项目 |
| 单个项目 · 粘贴正文 | 没有文件时用 |

### 流程页

| 卡片 | 干嘛的 |
|---|---|
| **这部剧有几集** | 集清单 + 每集的时长/段数/理由。**环节1 跑完必看** |
| **一键跑到底** | 主控制台。范围、并发、尺寸比例时长、预览、开始/停下 |
| 项目进度与续跑 | 每个环节做到哪儿了，以磁盘为准 |
| 这部剧到现在花了多少 | 用量账本汇总，跨多次续跑累计 |
| 项目参数 | 覆盖默认参数，只影响这个项目 |
| 十二环节 | 单独跑某一个环节（调试用） |
| 运行日志 | |

### 生产页

| 卡片 | 干嘛的 |
|---|---|
| 生产任务 | 三类活各一行：资产图 / 故事板 / 分段视频。可以单独跑、可以「补失败」 |
| 运行中的任务 | 全局视图，含其它项目。切项目不会中断 |
| 任务明细 | 点上面的任务卡看每一项的状态和日志 |

> 进度显示「0　没有任务」= 前置环节还没产出清单，按钮是灰的。
> 鼠标悬停会说是哪一步没做。

### 产物页

| 视图 | 干嘛的 |
|---|---|
| **资产库** | 全剧资产网格。每张卡有编号、名称、类别、父资产、四个状态灯（图/词/参N of M/用在几集）。点开看外观原文、生产提示词全文、要喂的参考图、花销 |
| **按段看** | 选一集，看这一集每个环节的完成度 + 一段一行的详情（剧情任务、进入退出状态、轴线、绑定的参考图、故事板、可播的视频、两份提示词全文） |
| 原始文件 | 按目录翻盘 |

### 设置页

见第 2 节。

---

## 5. 出错了怎么办

### 三级处理

| 级别 | 什么时候 | 程序会怎样 |
|---|---|---|
| **整批致命** | 余额不足、密钥无效、域名连不上 | **立刻停整条流水线**，剩下的步骤留 `pending`（表示压根没发出去） |
| **整集卡住** | 某集的环节2/3/4/6 失败 | 这一集后续跳过，**其它集继续** |
| **单项失败** | 某一段分镜/编译、某一张图、某一段视频 | 只这一项失败，同批其它照做 |
| 用户取消 | 你点了「停下」 | 标 `cancelled`，不算失败、不重试 |

### 每条报错都自带处理方法

面板上点开失败的那一步，会有：**标题 → 为什么 → 去哪改 → 怎么办（分步）→ 改完怎么续跑**。
全是大白话，没有术语。

### 几个最常见的

| 报错 | 真正的原因 | 怎么办 |
|---|---|---|
| **拆剧本的模型什么都没回** | 八成是并发打满 —— 很多网关限流时不报错，只回一个空回复 | **先把「分析·总上限」调到 3、「分析·集并发」调到 2**。只有一两集空、别的正常，基本就是这个 |
| **这一集有几段没做成，其余的都好了** | 环节7/8 是一段一次调用的，一段失败只影响那一段 | 先看**带段号的那条**记录（那才是真正的原因），处理完再点一次「开始」，只补没做成的那几段 |
| **这一集的段数和环节1 定的对不上** | 模型没按要求切 | 提醒级，不拦流程。介意的话删掉那一集的 `s2_segments.json` 重跑环节2 |
| **拆剧本的模型话没说完就被截断了** | 输出超过 max_tokens | 设置 → 分析引擎 → 单次输出上限调大。**这条不会自动重试**（重试必然同样被截断，只是把钱花三倍） |
| **参考图文件不存在** | 理论上不该再出现（资产图已按依赖分层出） | 出现了说明父资产那一层有失败，先修那一层 |
| **发得太快，被服务商拦了** | 出图出片的并发太高 | 设置 → 同时最多跑几个，把那家的配额调小 |

### 「没见过的错误」

说明我的分类表里没有这一条。把面板上的原文发我，我加进去。

---

## 6. 续跑与重跑

### 核心规则

**「这一步做过没有」只看磁盘上有没有产物，不看任何运行记录。**

所以：

- 进程杀了、服务重启了、换台电脑了 —— **再点一次「开始」就是继续**
- 做过的一步都不重做，**不会重复花钱**
- 写文件是原子的（先写 `.tmp` 再改名），**绝不会留下半个坏 JSON**

### 想重跑某一步 = 删掉它的产物

| 想重跑 | 删什么 | 注意 |
|---|---|---|
| 环节1（重新切集） | `01_剧本与分段/s1_global.json` + `episodes.json` | 后面所有环节的产物都会变得不匹配，建议整个项目清空 |
| 某一集的环节2 | `01_剧本与分段/EPxx/s2_segments.json` | 这一集后面的环节要跟着重跑 |
| 某一集的环节7 的某几段 | 编辑 `s7_shots.json` 删掉那几段的条目 | 只补那几段 |
| 某一集的环节8 的某几段 | 删 `03_提示词/故事板提示词/EPxx-SEGnn_*.txt` 和视频提示词 | 只补那几段 |
| 某张资产图 | 删 `02_固定资产/<类别>/<资产ID>.png` | |
| 某段故事板/视频 | 删 `04_故事板/` 或 `05_分段视频/` 里对应的文件 | |

> 资产图、故事板、视频**已存在就跳过不覆盖**（生成即固定）。想重出必须先删。

### 「补失败」按钮

「生产」页每行有个「补失败」—— 只重跑记录在案的失败任务，不动别的。

---

## 7. 并发怎么调

「一键跑到底」卡片上四个数字，管的是不同的东西：

| 输入 | 默认 | 管什么 |
|---|---|---|
| **出图并发** | 3 | 只管环节5b/9/10 的出图出片，管不到文字环节 |
| **分析·集并发** | 4 | 同时处理几集 |
| **分析·段并发** | 4 | 环节7、8 内部同时做几段 |
| **分析·总上限** | 6 | **LLM 在途请求的天花板**，上面两个乘起来可能很大，真正卡住的是这个 |

顶栏能看到实时在途数：`出图 2/8 · 分析 3/6 · 运行 1`。

### 调的原则

- **报「模型什么都没回」→ 把总上限调小**（6 → 3），集并发也降到 2
- **报「发得太快」→ 设置里把那家服务商的配额调小**
- 只测一集时集并发用不上（只有一集），起作用的是段并发
- 顶栏的「分析 x/y」长时间贴着上限 = 总上限就是瓶颈，可以试着加；一加就报错 = 到头了

### 有些东西不能并行，这是设计

| | 能不能并行 |
|---|---|
| 同一集内 环节2→3→4→6→7→8 | 不能，每步吃上一步的产物 |
| 跨集的环节4 | 不能，要沿用前面集已建好的资产编号（乱序会两集各给同一对象编号） |
| 环节3 的段 | 不能，它要逐对校验相邻段的状态衔接 |
| 环节7、8 的段 | **能**，已经在并行 |
| 其余跨集环节 | **能**，已经在并行 |

---

## 8. 花了多少钱

「流程」页的「这部剧到现在花了多少」卡片，按**环节 / 模型 / 总计**三个维度汇总，
跨多次续跑累计，重启不丢。

原始记录在 `07_检查与记录/usage.jsonl`，一行一次调用，只追加：

```json
{"kind":"llm","stage":"s7","episode":"EP01","target":"EP01-SEG03",
 "provider":"llm","model":"gpt-5.6-sol","prompt_tokens":7828,
 "cached_tokens":3840,"completion_tokens":4392,"seconds":107}
```

**填了计价表才显示金额**，不填只统计用量。

「资产库」里每张卡点开也能看到这一项自己花了多少（几次调用、多少 token、哪家）。

---

## 9. 换电脑 / 更新程序

### 三个目录分清楚

| | 放什么 | 更新程序时 |
|---|---|---|
| **程序目录** | 代码、提示词模板、页面 | **整个被覆盖** |
| **数据目录** | `config.json`（所有 key、凭证、优先级链、计价表） | 不碰 |
| **产物目录** | 剧本、资产图、故事板、视频（几十 GB） | 不碰 |

### 数据目录怎么定

按这个顺序，先命中的算：

```
1. python run.py --data D:\stv-data
2. 环境变量 STV_DATA_DIR
3. 程序目录里已经有 config.json —— 老装法，原地不动
4. %LOCALAPPDATA%\script-to-video-studio
```

设置页「数据与路径」卡片会显示当前是哪一种。**如果显示红框「配置文件在程序目录里」，
点一下「把配置搬出程序目录」** —— 它只复制 + 把原件改名成 `config.json.已搬走`，不删任何东西。

### 换到另一台电脑

```powershell
# 1. 把程序目录拷过去，或者 git clone
# 2. 设数据目录（一次性）
setx STV_DATA_DIR D:\stv-data
# 3. 起服务
python run.py
```

配置和产物都从那个目录读，两台机器各有一份、互不影响。

### 更新程序

直接覆盖程序目录 / `git pull`。**配置和产物不在里面，覆盖多少次都不会丢。**

### 打包成 exe（目标机器不用装 Python）

在**装了 Python 的机器上**打包一次，产物拷到别的机器直接跑：

```bash
pip install pyinstaller
python 打包exe.py            # 单文件，一个 exe 拿了就走（约 140 MB）
python 打包exe.py --onedir   # 一个文件夹，启动快、杀软误报少
```

产物在 `dist/`，手册也会被一起放进去，整个文件夹拷走即可。

打包脚本会先检查四个可选依赖装没装，缺哪个就告诉你 exe 里会少什么能力
（对象存储上传 / 参考图压缩 / 读 PDF / 拼接成片），确认后才继续。

**目标机器上怎么跑：**

```
script-to-video-studio.exe                      配置和产物放 exe 旁边（绿色版）
script-to-video-studio.exe --data D:\stv-data   放指定目录
script-to-video-studio.exe --port 8888          换端口
```

**绿色版模式**：在 exe 旁边放一个 `config.json`（内容 `{}` 就行），
程序就会把配置和 `projects/` 都放在 exe 那个文件夹里，整个文件夹拷到哪都能跑。
不放的话配置默认落在 `%LOCALAPPDATA%\script-to-video-studio`。

> 更新 exe 只是替换那一个文件，**旁边的 `config.json` 和 `projects/` 不受影响**，
> 所以绿色版不会像源码装法那样被「覆盖更新」弄丢配置。

**几个要知道的：**

| | |
|---|---|
| 位数 | 打包机器的位数决定 exe 的位数，64 位机打的不能在 32 位机上跑 |
| 杀软 | PyInstaller 打的包常被误报，第一次启动可能被拦，加信任即可 |
| 启动速度 | 单文件每次要解压，慢几秒；嫌慢用 `--onedir` |
| 临时目录 | 正常 Ctrl+C 退出会自己清理；被强杀会在 `%TEMP%` 留下 `_MEI*` 文件夹 |
| 拼接成片 | 靠打包进去的 `imageio-ffmpeg`，或者目标机自己装了 ffmpeg |
| 控制台 | 故意保留 —— 启动时的四行路径、跑批的进度和报错都在那儿 |

### 两台机器共用同一批产物

把「产物目录」指到同一个共享盘。判断「做过没有」只看磁盘产物，所以 A 机跑到第 12 集断了，
B 机点「开始」直接从第 13 集接着跑。

> ⚠️ **别让两台同时跑同一个项目** —— 会写同一批文件、把钱花两遍。
> 同一台机器上有拦截，跨机器拦不住。

---

## 10. 常见问题

**Q：环节1 要跑多久？**
40 集、8 万字的剧本大约 2-4 分钟。它一次读完全篇，是最贵的一次调用。

**Q：只想测一集，为什么环节1 还要读整部？**
因为人物表、视觉基调、集边界都得看全篇才准。第 8 集才砸毁的房间、第 3 集才揭晓的疤痕，
不在这一步进演化图，资产库中途就得返工。**这一步省不掉，也只跑这一次。**

**Q：一集大概多久、多少钱？**
按 EP01 实测（12 段）：并发跑约 20 分钟，29 次 LLM 调用。出图出片另算，取决于服务商。

**Q：「资产图 全剧共享」是什么意思？**
同一个角色跨集只出一张图（`02_固定资产/人物身份资产/C001.png`，**路径不含集号**），
这既省钱，更是跨集人脸一致的前提。所以资产图不按集出，只出你选的那几集用得到的。

**Q：状态资产（ST开头）是什么？**
角色/道具/场景的某个特定状态，比如「Aisyah 七年前无烧伤怀孕跪地状态」。
它**只描述与父资产的差异**，生成时要把父资产的图当参考图喂进去，脸才是同一个人。
所以资产图是**分层出**的：先出身份/场景/道具，再出依赖它们的状态资产。

**Q：为什么故事板和视频那两行是 0/0？**
环节8 还没编出提示词。按钮是灰的，鼠标悬停会说明。

**Q：改了提示词模板要重跑吗？**
改的是哪个环节的模板，就重跑哪个环节（删它的产物）。已经做好的其它环节不受影响。

**Q：产物能给 Claude Code / Codex 做验收吗？**
能，这是设计目标之一。把整个项目目录拖给它，用 skill 做验收与优化。

**Q：报错说「这个项目已经有一条流水线在跑了」？**
同一个项目不许并行两条 —— 两条都会写同一批产物文件，白花两份钱。
等它跑完（做过的下次会自动跳过），或者先点那条的「停下」。

**Q：能同时做几部剧吗？**
能。不同项目互不影响，共用「设置 → 同时最多跑几个」那个总闸。
