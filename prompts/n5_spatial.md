# 第五环节｜空间主表

> **本次只处理 {{EPISODE}} 这一集。** 空间是全剧共享的 ——
> 同一个地点在别的集出现过就沿用原编号，不要重建。

给每个地点建**一套坐标真相**：原点在哪、轴怎么定、单位是什么、有哪些区域、
锚点的实际坐标、通路怎么连、哪些结构固定不动。

## 一、为什么必须用坐标，不能用「画面左边」

这是本环节存在的全部理由。

「病床在画面左侧」是**某一个机位看到的样子**。换个机位它就在右边。
如果把它当成空间真相记下来，切镜时模型只能二选一：要么让病床跳到另一边，
要么让人物绕到另一侧 —— 两种都是错的，而且不报错。

```
✅ 病床：Zone=A 区，锚点 BED_01，坐标 [2.4, 0, 1.8]，床头朝 +X
❌ 病床：在画面左侧，靠近后景
```

**坐标属于物理真相，画面左右属于镜头。** 后面第九环节算画面左右时，
是拿机位去投影这套坐标，不是反过来。

## 二、本项目的空间做到哪一档

skill 给了四档，从强到弱：

```
真实 3D 块模 / 摄影测量
> 带尺寸的 2.5D 平面 + 轴测
> 纯文字坐标合同          ← 本项目做到这一档
> 各视角分别自由生图      （这一档不算过关）
```

**本项目不建 3D 代理**（出图模型做不出可靠的几何代理），所以你要做的是
**把文字坐标合同写扎实**：带尺寸的区域、明确的锚点坐标、成序列的通路、
以及多个视角之间共用的地标。

这一档的已知代价：同一场景多机位之间**不保证严格可拼合**。所以更要写清
`landmarks` —— 它们是不同视角之间唯一的对齐依据。

## 三、坐标合同要写什么

每个空间：

- `world_origin` —— 原点定在哪个真实位置（「主入口地面中心」这种能指认的地方），
  不要写 `[0,0,0]` 就完事。
- `axis` —— `+X` `+Y` `+Z` 各指向哪。写成人能核对的（「+X 指向窗户」）。
- `unit` —— 米。
- `zones` —— 有叙事意义的区域，每个给范围和它支持什么剧情行为
  （病房：床位区 / 探视区 / 门口区 / 窗边区）。
- `anchors` —— 固定物的锚点，**必须给 xyz**。`门边`、`床旁`不算锚点。
- `routes` —— 通路写成**锚点序列**，不是一句话。
- `landmarks` —— 不同机位都看得见、可以用来对齐的东西。
- `fixed_structures` —— 墙、门、窗、楼梯、电梯、大型家具，含开门方向。
- `scale_anchors` —— 已知尺寸的参照物（门高 2.1 米、病床长 2 米），
  模型靠它稳住比例。
- `forbidden_geometry_changes` —— 明写不许镜像翻转、不许移门窗、
  不许为了构图挪固定家具。

## 四、区分三种「变化」

| | 例子 | 怎么处理 |
|---|---|---|
| 永久几何 | 墙、门、窗、走廊、病床固定区 | 写进 `fixed_structures`，不许变 |
| 可修订几何 | 爆炸、倒塌、装修 | 建新的 `spatial_revision`，显式说明 |
| 临时空间状态 | 碎玻璃、封路、移动的障碍 | 写进 `temporary_obstacles`，走连续性总账 |

通路当前被障碍挡住时，**不能因为后面会移开就提前当成可用**。

## 五、机位覆盖（`loc_views`）

登记这一集需要从哪几个方向看这个空间。每个视角写：机位大致位置、朝向、
覆盖到哪几个 Zone、能看见哪几个地标。

**这一步只是登记需求，不出图。** 它的用处是让后面第九环节挑机位时，
知道哪些方向是已经想清楚的；镜头要求一个没登记过的方向时，
应该回到这里补，而不是在故事板里第一次发明房间另一侧长什么样。

## 输出 schema

```json
{
  "episode": "{{EPISODE}}",
  "spatial_masters": [
    {"spatial_id": "SP001",
     "location_asset_id": "S001",
     "name": "",
     "revision_note": "沿用/新建/因剧情事件修订（写清哪个事件）",
     "world_origin": "原点定在哪个真实位置，要能指认",
     "axis": {"x": "+X 指向什么", "y": "", "z": "通常是高度"},
     "unit": "meter",
     "outer_boundary": "整体尺寸，如 12m x 8m x 3m",
     "zones": [
       {"zone_id": "A", "name": "床位区", "extent": "范围描述或坐标范围",
        "supports": ["这个区域能承载哪些剧情行为"]}
     ],
     "anchors": [
       {"anchor_id": "BED_01", "name": "病床", "zone_id": "A",
        "xyz": [2.4, 1.8, 0], "orientation_yaw_deg": 90,
        "size": "2.0m x 0.9m x 0.6m",
        "relation": "床头贴 +X 侧墙，右侧距床头柜 0.4m"}
     ],
     "routes": [
       {"route_id": "R1", "path": ["DOOR_01", "A", "BED_01"],
        "description": "从门口到床边", "blocked_by": ""}
     ],
     "landmarks": ["不同机位都看得见、用来对齐的东西"],
     "fixed_structures": [
       {"kind": "door|window|wall|stair|elevator|furniture",
        "anchor_id": "DOOR_01", "xyz": [0, 4.0, 0],
        "opening_direction": "向内开，朝 +Y"}
     ],
     "scale_anchors": ["门高 2.1m", "病床长 2.0m"],
     "temporary_obstacles": [
       {"what": "", "zone_id": "", "blocks_route": "R1",
        "from_event": "哪个事件造成的", "until": "什么时候清除"}
     ],
     "forbidden_geometry_changes": [
       "禁止镜像翻转", "禁止移动门窗", "禁止为构图挪动固定家具"
     ]}
  ],
  "loc_views": [
    {"view_id": "SP001_V1", "spatial_id": "SP001",
     "camera_from": "大致机位位置或方向描述",
     "look_at": "看向哪",
     "covers_zones": ["A", "B"],
     "visible_landmarks": [""],
     "why_needed": "哪几场戏要用这个方向"}
  ],
  "space_continuity": [
    {"from_scene": "SC01", "exit_zone": "A", "to_scene": "SC02",
     "entry_zone": "B", "via_route": "R1",
     "inheritance": "下一场从哪个位置继续"}
  ],
  "coverage_note": "这一档是文字坐标合同，多机位不保证严格可拼合；靠 landmarks 对齐"
}
```

## 输入

【项目参数】
{{PARAMS}}

【本集叙事结构（第三环节）】
{{NARRATIVE}}

【本集资产（第四环节）】
{{ASSETS}}
