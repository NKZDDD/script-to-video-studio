# 第六环节｜连续性总账

> **本次只处理 {{EPISODE}} 这一集。**

记录这一集里**每一次状态变化**：什么事件造成的、改了谁的什么、
从什么变成什么、会持续多久、什么条件下结束。

## 一、只有一份状态真相

不要建「当前世界状态表」。唯一的逻辑是：

```
事件 → 状态变化 → 记进总账
                    ↓ 按时间和线程查询
              某一刻的世界状态
```

总账记的是**历史变化**；「现在是什么状态」是**查出来的结果**，
不是另一份可以单独编辑的数据。

**两份状态数据一定会打架**，而且打架时没人知道该信哪份。

## 二、一个事件同时改很多东西

```
砸碎瓶子
├── 道具：完整 → 破碎
├── 持有人：她 → 无
├── 空间：地面出现碎玻璃障碍
└── 人物：手可能被划伤
```

**这四条共享同一个事件来源和成立时间**，必须一起写进去。
让各资产系统各自推演的后果是：瓶子已经碎了但手里还拿着完整的瓶子，
地面障碍晚三个镜头才出现。

关键事件的**起因和结果原则上归属同一段**：枪响 → 中枪成立 →
这一段结束时人已经受伤。下一段从受伤继续，不重演开枪。

## 三、每条状态都要有生命周期

```
未激活 ──激活事件──→ 生效中 ──失活/替换/自然结束──→ 已结束
```

`persistence_class` 五档，决定它能持续多久：

| | 含义 |
|---|---|
| `INSTANT` | 只属于事件那一瞬间 |
| `SCENE_LOCAL` | 本场戏内持续 |
| `CROSS_SCENE` | 跨场次持续，直到明确结束 |
| `LONG_TERM` | 跨集持续 |
| `PERMANENT` | 不可逆 |

**失活必须有原因** —— 事件、时间差，或者写好的自然规律。
湿衣服可以因为换衣、烘干、时间够久而结束；
**伤口、烧焦的墙、破损的衣服不能因为「下一个镜头不方便」而消失**。

## 四、两条时间铁律

**已发生的会一直继承。** 画面暂时看不见 ≠ 已经失活。
被袖子挡住的伤口还在那儿，下一个镜头露出来时必须还是同一个伤口。

**未发生的绝对不许提前出现。** 激活事件之前，这个状态没有任何视觉权力 ——
完整的、局部的、弱化的、模糊的、预示的、融合的，一种都不行。

剧情确实需要预示时，**把预示本身单独定义成一个合法状态**，
而不是把未来状态漏出来一点点说这是预示。

## 五、「存在」和「看得见」是两回事

每条状态在每个时间点标一个可见性：

```
明确可见 / 部分可见 / 被遮挡但仍存在 / 画外但仍存在 / 尚未激活（禁止出现）
```

**不要为了展示左手的伤而破坏走位。** 该记「被遮挡但仍存在」，
后面重新看得见时恢复同一个状态。

道具尤其要对账：

```
存在总数 = 明确可见 + 部分可见 + 被遮挡 + 画外
```

遮挡、离画、装进容器**都不改变存在数量**。总数只能由创建、进入/离开世界、
损毁、消耗、拆分、合并这些事件改变。

## 六、持有人、归属、容器不是一回事

- `holder` —— 现在**直接拿着**它的人
- `owner` —— 它**属于**谁
- `container` —— 它**装在**什么里面

持有人变化必须和双方的手部占用、实例位置、状态同步改 ——
文件从她手里到他手里，她的手就空出来了。

## 输出 schema

```json
{
  "episode": "{{EPISODE}}",
  "ledger": [
    {"event_id": "EV012",
     "story_time": "故事内时间",
     "reality_thread": "RT_MAIN",
     "source_scene": "SC02", "source_beat": "SC02-B3",
     "affected_entity": "C001 或 PI001 或 SP001",
     "state_dimension": "外观|持有|空间|身体|环境|情绪",
     "previous_value": "",
     "delta": "改变了什么",
     "result_value": "变成什么",
     "activation_event": "由什么触发（就是本条 event 的描述）",
     "persistence_class": "INSTANT|SCENE_LOCAL|CROSS_SCENE|LONG_TERM|PERMANENT",
     "deactivation_condition": "什么条件下结束；PERMANENT 填「不可逆」",
     "natural_decay_rule": "会自然消退的写规律，否则空",
     "visual_asset_binding": "对应哪条状态资产；没有对应资产填空",
     "visibility_by_seg": [
       {"seg_id": "{{EPISODE}}-SEG03",
        "visibility": "明确可见|部分可见|被遮挡|画外|尚未激活"}
     ]}
  ],
  "prop_tracking": [
    {"instance_id": "PI001",
     "timeline": [
       {"seg_id": "{{EPISODE}}-SEG01", "holder": "C005", "hand": "右手",
        "owner": "C005", "container": "", "zone": "A",
        "state": "完整", "visibility": "明确可见"}
     ],
     "count_lock": {"active_total": 1,
                    "reconciliation": "可见1 + 部分0 + 遮挡0 + 画外0 = 1"}}
  ],
  "synchronized_deltas": [
    {"event_id": "EV012",
     "deltas": ["同一事件造成的多条变化，列出 affected_entity"],
     "note": "为什么它们必须同时成立"}
  ],
  "forbidden_future_by_seg": [
    {"seg_id": "{{EPISODE}}-SEG01",
     "must_not_appear": ["这一段绝对不许出现的未来状态"]}
  ],
  "carry_in": "从上一集继承进来的、仍然生效的状态",
  "carry_out": "本集结束时仍然生效、下一集要继承的状态"
}
```

## 输入

【项目参数】
{{PARAMS}}

【本集叙事结构（第三环节）】
{{NARRATIVE}}

【本集资产（第四环节）】
{{ASSETS}}

【本集空间主表（第五环节）】
{{SPATIAL}}
